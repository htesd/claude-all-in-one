# KiroManager 激活端指纹 — 对齐参考(防封)

| 能力点/字段 | 参考来源(文件:行号 函数/字段名) | 它做什么 | 不对齐会怎样 | kiro-gw对齐落点 | 建议Phase | confidence |
|---|---|---|---|---|---|---|
| machineId 总入口按 OS 真实值读取 | `main/machineId.ts:91 getCurrentMachineId`, `main/machineId.ts:313 getWindowsMachineId`, `main/machineId.ts:386 getMacOSMachineId`, `main/machineId.ts:458 getLinuxMachineId` | 激活端优先读取宿主机真实 machine identity，而不是纯随机值；Windows 读 `MachineGuid`，macOS 读 override/Kiro 文件再回退硬件 UUID，Linux 读 `/etc/machine-id` 或 dbus machine-id 并格式化成 UUID。 | 激活端和 gw 发包端若 machineId 来源不同，会形成“双指纹漂移”；同一账号先激活再使用时被云端视为换设备/换客户端，封号风险最高。 | `gw-kiro/machine_id` | P1 | high |
| 随机 machineId 只作为生成能力，不是默认激活来源 | `main/machineId.ts:83 generateRandomMachineId` | 提供 GUID 生成函数，但实际读取当前机器码走 OS 真实值链路。 | gw 若偷懒每号随机一个 machineId，而激活端是 OS 真值，会立刻与激活痕迹脱锚。 | `gw-kiro/machine_id` | P1 | high |
| macOS 优先读/写 Kiro 本地 machineid 覆盖文件 | `main/machineId.ts:386 getMacOSMachineId`, `main/machineId.ts:424 setMacOSMachineId` | macOS 不直接改硬件 UUID，而是优先用 `userData/machine-id-override`，并同步 `~/Library/Application Support/Kiro/machineid`。 | gw 如果只认硬件 UUID、不认 Kiro 本地覆盖值，会与激活端实际提交的设备 ID 不一致。 | `gw-kiro/machine_id`, `gw-store` | P1 | high |
| Linux 实际基于 OS machine-id，写回系统文件 | `main/machineId.ts:458 getLinuxMachineId`, `main/machineId.ts:479 setLinuxMachineId`, `main/machineId.ts:515 setLinuxMachineIdWithPkexec` | Linux 读取系统 machine-id，写入时去掉连字符并尝试同步 `/etc/machine-id` 与 `/var/lib/dbus/machine-id`。 | gw 若把 Linux 设备号当作任意 UUID，会和激活端提交的格式/值都不一致，触发设备漂移。 | `gw-kiro/machine_id` | P2 | medium |
| 浏览器身份固定伪装为 Windows Chrome UA | `main/registration/browser-identity.ts:179 randomIdentity` | 每次注册生成一套浏览器身份，但 UA 恒为 `Windows NT 10.0; Win64; x64` 的 Chrome。 | gw 若按实际服务器 OS 发 Linux/macOS UA，而激活端是 Windows Chrome，会出现注册端和后续调用端平台人格不一致。 | `gw-kiro/fingerprint`, `gw-kiro/headers` | P1 | high |
| Chrome 详细版本随机于 137-146 稳定版区间 | `main/registration/browser-identity.ts:159 randomChromeVersion`, `main/registration/browser-identity.ts:179 randomIdentity` | Chrome 版本不是常量，按真实稳定版 `major.0.build.patch` 随机。 | gw 若长期固定单一老版本或版本结构不真实，容易与激活端留下的版本分布不一致，增加风控特征。 | `gw-kiro/fingerprint`, `gw-kiro/headers` | P2 | medium |
| GPU 指纹池 | `main/registration/browser-identity.ts:4 GPU_CONFIGS`, `main/registration/browser-identity.ts:179 randomIdentity` | 从 Intel/NVIDIA/AMD 真实 ANGLE D3D11 组合池中选 GPU vendor/model。 | gw 若不用同类 GPU 语义或发送服务器侧 WebGL 值，会和激活端浏览器指纹画像脱节。 | `gw-kiro/fingerprint` | P2 | high |
| 屏幕分辨率/色深池 | `main/registration/browser-identity.ts:29 SCREEN_CONFIGS`, `main/registration/browser-identity.ts:179 randomIdentity` | 生成常见桌面分辨率、availHeight、24 位色深。 | gw 若没有同类屏幕画像或出现不合理尺寸，会形成“激活端桌面浏览器，后续端异常设备”的割裂。 | `gw-kiro/fingerprint` | P3 | medium |
| WebGL 扩展、canvas hash、math 指纹 | `main/registration/browser-identity.ts:38 MATH_POOL`, `main/registration/browser-identity.ts:51 WEBGL_EXT_CORE`, `main/registration/browser-identity.ts:121 generateCanvasData`, `main/registration/browser-identity.ts:179 randomIdentity` | 生成 canvas hash/histogram、math 三元组、WebGL 扩展集合。 | gw 若后续要模拟激活端浏览器人格，这些字段不对齐会让同账号在风控侧像“换浏览器内核/换设备”。 | `gw-kiro/fingerprint` | P3 | medium |
| 插件列表与 PDF viewer 画像 | `main/registration/browser-identity.ts:71 PLUGINS_POOL`, `main/registration/fingerprint.ts:233 formatPlugins`, `main/registration/fingerprint.ts:333-336 buildFingerprintData` | 指纹里把 PDF Viewer 类插件名拼进 `plugins/dupedPlugins/screenInfo`。 | gw 若完全没有该类插件画像，可能与激活时浏览器 blob 不连续。 | `gw-kiro/fingerprint` | P3 | medium |
| 指纹 blob 主体字段清单 | `main/registration/fingerprint.ts:239 buildFingerprintData` | 加密前按固定顺序写入 `metrics/start/interaction/scripts/history/battery/performance/automation/end/timeZone/plugins/screenInfo/lsUbid/referrer/userAgent/location/webDriver/capabilities/gpu/dnt/math/timeToSubmit/form/canvas/token/auth/errors/version`。 | gw 若要复刻激活期浏览器指纹，此结构是核心契约；字段缺失/顺序飘移都可能让服务端判为非官方注册端。 | `gw-kiro/fingerprint` | P1 | high |
| 指纹内 userAgent/location/referrer 与页面类型绑定 | `main/registration/fingerprint.ts:253-339 buildFingerprintData`, `main/registration/registrar.ts:755-782 genFPWithTime` | 根据 `signin/signup/profile` 页面和事件类型生成 location/referrer，再把 `identity.ua` 嵌入 blob。 | gw 若后续还原浏览器侧行为时不保留这一套页面语义，服务端可能看到不合理跳转链。 | `gw-kiro/fingerprint` | P2 | high |
| 指纹加密输出 | `main/registration/fingerprint.ts:387 generateFingerprint` | 将构造好的 JSON 指纹经 `encryptFingerprint` 变成最终 `fingerPrint` 字符串上送。 | gw 若要做完全对齐，不能只抄明文字段名，必须兼容加密输出格式。 | `gw-kiro/fingerprint` | P2 | medium |
| visitorId / awsccc / ubid / kiroVisitorId / PKCE 生成器 | `main/registration/http-utils.ts:17 visitorId`, `main/registration/http-utils.ts:22 awsccc`, `main/registration/http-utils.ts:35 ubidGen`, `main/registration/http-utils.ts:42 kiroVisitorId`, `main/registration/http-utils.ts:50 pkce` | 激活流程会生成多种浏览器态/会话态标识和 cookie 值；`awsccc` 为 base64 JSON，`ubid` 为 `186-xxxxxxx-xxxxxx` 样式。 | gw 若以后补齐激活/登录/SSO 模拟，这些 ID/cookie 语义不对会导致会话人格断裂。 | `gw-kiro/fingerprint`, `gw-kiro/token` | P2 | high |
| TLS 指纹固定为 chrome_146 | `main/registration/registrar.ts:253-279 sessionOpts`, `main/registration/registrar.ts:269 tlsClientIdentifier: 'chrome_146'` | 所有注册请求通过 `tlsclientwrapper` 的 `chrome_146` TLS 指纹发出。 | 这是最关键的网络层指纹之一；gw 若走默认 Rust/Node TLS 栈，JA3/握手特征会与激活端完全不同，极易触发风控。 | `gw-app/egress` | P1 | high |
| TLS ModuleClient 为进程级共享池 | `main/registration/tlsClientPool.ts:29 acquireModuleClient`, `main/registration/registrar.ts:291-300 initTlsClient` | 首次 open 共享 ModuleClient，后续每次注册只新建 SessionClient，保持同套 TLS 库能力。 | gw 若每次请求都随意切换 TLS 实现或 session 形态，网络指纹稳定性更差。 | `gw-app/egress` | P3 | medium |
| 注册请求 HTTP 头（signin/signup API） | `main/registration/registrar.ts:441 buildHeaders` | 统一发送 `Accept-Language: zh-CN,zh;q=0.9,en;q=0.8`、`User-Agent=identity.ua`、动态 `sec-ch-ua`、`sec-ch-ua-platform: "Windows"`、`sec-fetch-*`、可选 `Origin/Referer/Cookie`。 | gw 如果只对齐 UA、不对齐 sec-ch-ua/sec-fetch/platform/语言头，仍会被看成另一类客户端。 | `gw-kiro/headers` | P1 | high |
| Profile API 专用头与 cookie 子集 | `main/registration/registrar.ts:461 buildProfileHeaders` | 对 profile 域请求改用 `Accept: */*`、`Content-Type: application/json;charset=UTF-8`、`priority: u=1, i`，并只带 `awsccc/aws-user-profile-ubid/i18next/awsd2c-token` 子集。 | gw 若 profile 域请求头/cookie 组合错乱，会破坏官方端页面流转特征。 | `gw-kiro/headers`, `gw-kiro/token` | P2 | high |
| OIDC 客户端注册 | `main/registration/registrar.ts:786-816 step1OIDC` | 第一步对 `/client/register` 注册 public client，scopes 为 `codewhisperer:*` 五项。 | gw 若自己做激活/刷新而 client 注册语义不同，后续 token 生态与官方端不连续。 | `gw-kiro/token` | P2 | high |
| Device authorization | `main/registration/registrar.ts:818-829 step2Device` | 第二步请求 `device_authorization`，绑定 `clientId/clientSecret/startUrl`。 | gw 若不按同一步序拿到 deviceCode/userCode，而是直接拼其他链路，风控与授权状态都可能偏离。 | `gw-kiro/token` | P2 | high |
| Portal 初始化与 awsd2c token | `main/registration/registrar.ts:887-913 step4Portal`, `main/registration/registrar.ts:712-747 fetchD2CToken` | 进入 portal/login，拿 `workflowStateHandle`、`loginCsrfToken`，再调用 `vs.aws.amazon.com/token` 换 `awsd2c-token` 并从 JWT 提取 `vid`。 | gw 若缺少这层浏览器态和 token 过渡，官方云端看到的登录链会与真实客户端不同。 | `gw-kiro/token`, `gw-kiro/fingerprint` | P2 | high |
| 工作流初始化 first_load/PageLoad | `main/registration/registrar.ts:916-950 step5WorkflowInit` | 对 signin execute API 先发 `FingerPrintRequestInput(first_load)`，若返回 `start` 再发 `PageLoad`。 | gw 若想重放官方激活时序，不能只发最终提交；前置 fingerprint 事件也是行为特征。 | `gw-kiro/fingerprint`, `gw-kiro/token` | P2 | high |
| 邮箱判定分流：login vs signup | `main/registration/registrar.ts:952-984 step6SubmitEmail` | `get-identity-user` 带 `UserRequestInput + ApplicationTypeRequestInput(SSO_INDIVIDUAL_ID) + UserEventRequestInput + FingerPrintRequestInput`；400 走 signup，200 走 login。 | gw 若账户激活/补登流程不按同样分流，可能走到和官方端不同的行为树。 | `gw-kiro/token` | P2 | high |
| Signup init + workflowID 获取 | `main/registration/registrar.ts:987-1012 step7Signup`, `main/registration/registrar.ts:1015-1062 step7_5SignupInit` | 先触发 `SIGNUP`，再对 signup API 做 `first_load`/`PageLoad`，从 redirect 里拿 `workflowID`。 | gw 若直接跳过 signup 页面阶段，会缺少官方注册端关键行为脚印。 | `gw-kiro/token`, `gw-kiro/fingerprint` | P2 | high |
| Profile 初始化、ubid、i18next、awsccc | `main/registration/registrar.ts:1065-1080 step7_8ProfileInit` | 设置 `aws-user-profile-ubid`、`i18next=zh-CN`、必要时补 `awsccc`，访问 profile 页并刷新 d2c token。 | gw 若后续登录态不保留这些浏览器态，会与激活端 profile 流程断层。 | `gw-kiro/token`, `gw-kiro/headers` | P2 | high |
| Start / send-otp / create-identity 行为事件 | `main/registration/registrar.ts:1083-1102 step8ProfileStart`, `main/registration/registrar.ts:1105-1143 step9SendOTP`, `main/registration/registrar.ts:1162-1186 step11CreateIdentity` | Profile 阶段持续带 `browserData.attributes`：`fingerprint/eventTimestamp/timeSpentOnPage/pageName/eventType/ubid/visitorId`。 | gw 若后续要完整复刻官方激活，需要把这套 page event 结构作为行为基线；缺它会像“只会 API 不会页面”的机器人。 | `gw-kiro/fingerprint`, `gw-kiro/token` | P3 | medium |
| 设置密码前先取加密公钥 | `main/registration/registrar.ts:1189-1249 step12SetPassword` | 先从 `encryptionContextResponse.publicKey` 拿公钥与 `issuer/audience/region`，再加密密码提交。 | gw 如果自己实现激活/续登，这一步协议不对会直接失败，且与官方端加密路径不一致。 | `gw-kiro/token` | P3 | medium |
| SSO workflow 与 sso-token | `main/registration/registrar.ts:1285-1312 step12_8SSOWorkflow`, `main/registration/registrar.ts:1377-1442 step13SSOToken` | 注册成功后走 portal/login → signin execute → `/auth/sso-token` → OIDC accept/associate/token 轮询，拿最终 access/refresh token。 | gw 若 token 获取链路和官方端不一致，后续 refresh/登出/风控关联都会飘。 | `gw-kiro/token` | P1 | high |
| IdC 与 social 的 profileArn 分流写盘 | `main/kiroAuthSync.ts:38 KIRO_BUILDER_ID_PLACEHOLDER_ARN`, `main/kiroAuthSync.ts:40 KIRO_SOCIAL_PROFILE_ARN`, `main/kiroAuthSync.ts:58 resolveProfileArnForWrite` | social/Github/Google 写固定 social profileArn；BuilderId/其它写官方占位 placeholder ARN；显式真实 ARN 优先。 | gw 若把 social 和 IdC 混写，或错误剥离/伪造 profileArn，会让账号身份类型与官方端记录不一致。 | `gw-kiro/token` | P1 | high |
| token 文件结构与 IdC client registration 文件 | `main/kiroAuthSync.ts:103 computeClientIdHash`, `main/kiroAuthSync.ts:116 writeKiroAuthTokenFile` | token 写入 `~/.aws/sso/cache/kiro-auth-token.json`；IdC 还会按 `clientIdHash(startUrl)` 写 `{clientIdHash}.json` 客户端注册文件，权限 0600，scopes 为同一组 OIDC scopes。 | gw 若导入/写盘格式不兼容，Kiro IDE 自己 refresh 时会失配，造成 token 轮换混乱和掉号。 | `gw-store`, `gw-kiro/token` | P1 | high |
| token 文件监听与单一真源 | `main/kiroAuthSync.ts:232 watchKiroAuthTokenFile` | 用 `fs.watchFile` 轮询监听 token 文件，基于 `accessToken|refreshToken` 去重，同步 IDE 与管理器状态。 | gw 若另起一套 token 真源，不与磁盘单一来源对齐，会再次出现 IDE/服务端 token 漂移。 | `gw-store`, `gw-kiro/token` | P1 | high |
| 发往 Kiro API 的 User-Agent / x-amz-user-agent | `main/proxy/kiroApi.ts:170 getKiroUserAgent`, `main/proxy/kiroApi.ts:175 getKiroAmzUserAgent`, `main/proxy/kiroApi.ts:1162 getAuthHeaders` | 非 IdC 请求带 `aws-sdk-js/... api/codewhispererstreaming... m/E KiroIDE-<version>-<machineId>` 与对应 `x-amz-user-agent`；并发 `x-amzn-kiro-agent-mode`。 | 这是 gw 发包端最核心的应用层指纹；如果 machineId、版本、SDK 族不对，云端会直接看到不是同一类官方客户端。 | `gw-kiro/headers`, `gw-kiro/machine_id` | P1 | high |
| IdC 与 social/BuilderId 的 UA/agent-mode 分流 | `main/proxy/kiroApi.ts:1162 getAuthHeaders` | `authMethod=idc` 时改发 Rust CLI 风格 UA 与 `agentMode=vibe`；否则发 JS IDE 风格 UA 与 `agentMode=spec`。 | gw 若所有账号都发同一套头，会把 CLI/IDE、IdC/social 身份揉成一团，极易形成异常画像。 | `gw-kiro/headers`, `gw-kiro/token` | P1 | high |
| 流式请求对 placeholder profileArn 特判剥离 | `main/proxy/kiroApi.ts:206 resolveProfileArn`, `main/proxy/kiroApi.ts:1226-1231 callKiroApiStream` | BuilderId 占位 ARN 在流式端点会 403，因此只传真实 ARN 或 social 固定 ARN，占位值要删掉。 | gw 若盲传 placeholder ARN 到流式端点，会出现官方端不会出现的 403/重试模式，也暴露实现差异。 | `gw-kiro/token`, `gw-kiro/headers` | P1 | high |
| 模型列表请求也带 machineId 风格 UA 与 profileArn | `main/proxy/kiroApi.ts:1970 fetchKiroModels` | 查询 `ListAvailableModels` 时带 `Authorization`、KiroIDE 风格 UA、`x-amzn-codewhisperer-optout:true`，并按账号身份附带 profileArn。 | gw 若这些周边接口的头部人格和主聊天接口不一致，也可能被后端拼图出“非官方混合客户端”。 | `gw-kiro/headers`, `gw-kiro/token` | P2 | high |
| 订阅/额度/偏好接口头部人格 | `main/proxy/kiroApi.ts:2052 fetchAvailableSubscriptions`, `main/proxy/kiroApi.ts:2099 fetchSubscriptionToken`, `main/proxy/kiroApi.ts:2148 setUserPreference` | 订阅相关接口换成另一套 `aws-sdk-js/1.0.0 ... api/codewhispererruntime... KiroIDE-<version>-<machineId>` UA，但仍绑定同一 machineId 与 profileArn。 | gw 若只对齐聊天接口、不对齐订阅和偏好接口，账号管理面仍会暴露第二套指纹。 | `gw-kiro/headers`, `gw-kiro/machine_id`, `gw-kiro/token` | P2 | high |
| 验活会刷新 token 并查询 usage limits | `main/registration/registrar.ts:1447 verifyAlive`, `main/registration/registrar.ts:1463-1479 verifyAlive` | 激活完成后立刻走 refresh token，再调用 `getUsageLimits`（us-east-1 / eu-central-1）做活性、suspended、额度校验。 | gw 若导入后不延续这种 token 生命周期和额度查询人格，账号管理链路仍可能与官方端不连续。 | `gw-kiro/token`, `gw-kiro/headers` | P3 | medium |
| 注册主时序总览 | `main/registration/registrar.ts:1518 run` | 完整顺序为：代理链/TLS/出口 IP → OIDC → Device → Email → Portal → WorkflowInit → SubmitEmail → Signup 流程 → OTP → CreateIdentity → SetPassword → SSO Workflow → SSO Token → VerifyAlive。 | 这是你写 gw 激活兼容层时的总行为蓝图；若后续 server 端需要重建激活环境，任何顺序压缩或跳步都可能与官方端相差过大。 | `gw-kiro/token`, `gw-kiro/fingerprint`, `gw-app/egress` | P1 | high |
