# 旧 kiro.rs 搬运资产 — 实现参考(搬运非重写)

| 资产 | 旧位置(文件:行号 函数名) | 是什么 | 为什么不能重写要搬运 | 搬到claude-all-in-one模块 | 建议Phase | confidence |
|---|---|---|---|---|---|---|
| AWS eventstream 单帧解析 | `src/kiro/parser/frame.rs:63-153 (parse_frame)` | AWS Event Stream 单帧解析，处理 prelude、header/payload 边界、prelude CRC、message CRC，并产出 `Frame { headers, payload }` | 这是 Kiro 上游流协议的底层兼容实现，长度/CRC/边界一旦偏一点就会整流失真；已在生产和测试里验证过，不值得重摸协议细节 | `gw-kiro/parser` | P1 | high |
| AWS eventstream Header 解析 | `src/kiro/parser/header.rs:130-182 (parse_headers)`; `src/kiro/parser/header.rs:186-257 (parse_header_value)` | 解析 AWS eventstream header 名称、类型和值，覆盖 string/int/uuid/bytearray 等协议字段，并封装 `Headers` 访问器 | 这是事件类型识别的根基，直接影响 `:message-type` / `:event-type` / `:exception-type` 识别；重写极易漏类型或边界校验 | `gw-kiro/parser` | P1 | high |
| AWS eventstream CRC 计算 | `src/kiro/parser/crc.rs:7-19 (crc32)` | 基于 ISO-HDLC 的 CRC32 计算，供 prelude/message CRC 校验复用 | CRC 常量和算法必须与协议完全一致，错一个 variant 会出现“看起来能读流、实际全坏”的隐蔽 bug | `gw-kiro/parser` | P1 | high |
| AWS eventstream 流式 decoder 状态机 | `src/kiro/parser/decoder.rs:110-220 (EventStreamDecoder::{new,feed,decode,decode_iter})`; `src/kiro/parser/decoder.rs:223-291 (try_recover)` | 带缓冲区、四态状态机、容错恢复的流式解码器；支持 chunk 喂入、帧级恢复、错误阈值停机 | Kiro 流不是一次性完整包，生产里必须抗分片/半包/坏包；恢复策略已踩坑验证，重写很容易把“偶发坏帧可恢复”变成全流崩 | `gw-kiro/parser` | P1 | high |
| thinking 签名 protobuf 重写 | `src/anthropic/signature.rs:65-133 (rewrite_message)`; `src/anthropic/signature.rs:143-152 (rewrite_model_in_signature)` | 对上游真实 thinking signature 的 protobuf 做定点改写，只替换 `f2.f1.f6` 模型代号，其余加密字节原样保留 | 这是 v42 身份探针修复核心，依赖已逆出的 protobuf 结构；随便重写很容易破坏真实签名布局，直接掉检测分 | `gw-kiro/stream` 或 `gw-kiro/signature` | P1 | high |
| thinking 签名合成兜底 | `src/anthropic/signature.rs:171-188 (derive_bytes)`; `src/anthropic/signature.rs:191-199 (encode_varint_field, encode_bytes_field)`; `src/anthropic/signature.rs:215-241 (synthesize_signature)` | 当上游不发原生 signature 时，合成一个结构合法、模型名正确、随 thinking 文本稳定变化的 protobuf 签名 | 这是 fake `<thinking>` 路径不至于“空签名裸奔”的关键补丁，结构细节和长度标定都来自实测；重写容易生成能 base64 但过不了探针的假货 | `gw-kiro/stream` 或 `gw-kiro/signature` | P2 | high |
| 原生 reasoningContentEvent → Anthropic thinking 块 | `src/anthropic/stream.rs:916-982 (process_reasoning_content)`; `src/anthropic/stream.rs:990-1015 (close_reasoning_block_if_open)` | 把 Kiro 原生 reasoning 事件转成 Anthropic thinking block，处理“签名先到/正文后到”、顺序保护、块关闭时 `signature_delta` 透传 | 这部分同时绑定协议顺序和检测兼容；若自己重写，最容易在“迟到 reasoning”“close 前补 signature_delta”这类边角回归 | `gw-kiro/stream` | P1 | high |
| fake `<thinking>` 标签流解析 | `src/anthropic/stream.rs:68-99 (find_real_thinking_end_tag)`; `src/anthropic/stream.rs:115-142 (find_real_thinking_end_tag_at_buffer_end)`; `src/anthropic/stream.rs:148-171 (find_real_thinking_start_tag)`; `src/anthropic/stream.rs:1018-1154 (process_content_with_thinking)` | 处理 Kiro 把 thinking 混在正文里的模型，识别真正标签、跳过引用标签、处理跨 chunk 半标签、从 text 中抠出 thinking 块 | 这是大量线上边角 case 堆出来的字符串状态机；不搬运的话，极容易重新踩“把 `</thinking>` 当正文发出”或“thinking/text 块顺序非法” | `gw-kiro/stream` | P1 | high |
| fake thinking 关闭时补签名 | `src/anthropic/stream.rs:1217-1233 (create_thinking_delta_event)`; `src/anthropic/stream.rs:1238-1256 (close_fake_thinking_block)` | fake thinking 路径累计 thinking 文本，关闭块时优先透传真实签名，否则调用 `synthesize_signature` 补 `signature_delta` | 这是 v42/v43 之后 thinking block 结构合规的闭环；漏掉会直接出现 thinking 块非法、探针报签名空缺 | `gw-kiro/stream` | P1 | high |
| SSE 状态机与块顺序约束 | `src/anthropic/stream.rs:279-538 (SseStateManager)` | 管理 `message_start/content_block_start/delta/stop/message_delta/message_stop` 的合法顺序，并清理半开块 | Anthropic SSE 时序要求非常严格；这套状态机已经内化了 thinking/tool_use/text 的顺序约束，重写很容易产出客户端拒收流 | `gw-kiro/stream` | P1 | high |
| StreamContext 流式聚合上下文 | `src/anthropic/stream.rs:543-610 (StreamContext fields)`; `src/anthropic/stream.rs:649-779 (new_with_thinking, setters, create_message_start_event, produced_any_content)` | 流式转译的中心状态：usage、thinking、tool_use、sim_cache、reasoning_signature、failure 等都在这里汇总 | 不是简单 DTO，而是整条流处理状态机的共享内存布局；后续 handler/stream/billing 都耦合这套字段语义 | `gw-kiro/stream` | P1 | high |
| 流终态失败分类 | `src/anthropic/stream.rs:612-647 (StreamFailure)` | 区分 `UpstreamError/UpstreamException/EmptyResponse/StreamIo`，统一日志分类与客户端 error 事件文案 | 这是 empty-response 与真上游错误分流的关键语义层，后续调度与健康惩罚都依赖此分类 | `gw-kiro/stream` | P2 | high |
| empty-response buffered fallback 主流程 | `src/anthropic/handlers.rs:598-756 (create_sse_stream)` | v58 流式建流核心：延迟 `message_start`、首包 empty 时不立刻结束，而是走 buffered fallback；同时处理 ping、事件 flush 与最终落库 | 这是“流式首包掐断但同号 buffered 能成功”的血泪修复主链，顺序和落库策略都很脆，不能按常规 SSE 逻辑重写 | `gw-kiro/stream` + `gw-app/router` | P1 | high |
| buffered fallback 执行器 | `src/anthropic/handlers.rs:810-953 (attempt_buffered_fallback)` | 首包 empty 时先记原 empty 记录，再同会话/同组走非流式重发；成功转 SSE，失败才 `report_empty_response` 冷却账号 | 这里内含“先兜底、后惩罚”的关键因果顺序，直接关系账号健康误杀与用户无感恢复；重写最容易把健康号提前冷却掉 | `gw-kiro/stream` + `gw-kiro/scheduler` | P1 | high |
| empty fallback 终态 error 事件 | `src/anthropic/handlers.rs:955-966 (empty_error_events)`; `src/anthropic/handlers.rs:776-797 (prepend_initial_if_unstarted)`; `src/anthropic/handlers.rs:758-774 (finalize_stream_failure)` | 兜底失败时补发合法终态 error/SSE 序列；若 `message_start` 尚未 flush，先补齐初始事件避免非法事件顺序 | 这是 v58 另一半：不只是“能兜底”，还要确保失败时也不产出 Anthropic 非法流；这些顺序坑线上已经踩过 | `gw-kiro/stream` | P2 | high |
| buffered 非流式统一解析器 | `src/anthropic/handlers.rs:994-1171 (parse_buffered_response)` | 一次性解完整 eventstream，抽取 text/reasoning/tool_use/contextUsage/metering/tokenUsage，产出统一 `BufferedParseResult` | 这是 live stream fallback 和非流式响应的共享核心，等于把 Kiro 事件语义压成内部 IR；重写会重复踩 token/stop_reason/signature 细节 | `gw-kiro/stream` 或 `gw-kiro/parser` | P1 | high |
| buffered 结果转 Anthropic SSE | `src/anthropic/handlers.rs:1180-1288 (buffered_result_to_sse_events)` | 把完整 buffered 结果伪装成规范 Anthropic SSE：thinking/text/tool_use 块逐块 start→delta→stop，再发 `message_delta/message_stop` | fallback 成功后要无感回灌给 SSE 客户端，这里定义了“完整响应如何重放成流”；顺序和 usage 结构都已验证 | `gw-kiro/stream` | P2 | high |
| 空 content 400 当前轮兜底 | `src/anthropic/converter.rs:598-619 (current message content fallback in convert_request)`; `src/anthropic/converter.rs:1082-1097 (EMPTY_CONTENT_PLACEHOLDER, MEDIA_ONLY_PLACEHOLDER)` | 处理 Kiro 对空 content 的 400：媒体无文本补 `Please analyze the attached file.`，彻底空回合补单空格，纯 tool_results 保持空文本 | 这是 v41/v54 的生产止血逻辑，规则细分来自真实 400 行为；若简化成“统一补空格”会再次污染媒体分析或工具回合 | `gw-kiro/converter` | P1 | high |
| 空 content 400 历史 user 兜底 | `src/anthropic/converter.rs:1326-1382 (merge_user_messages)` | 合并历史 user 消息时沿用当前轮规则：媒体无文本补引导语，全空补空格，仅 tool_results 不补 | Kiro 不只检查 currentMessage，也会因为历史里的空 user message 整会话 400；这类毒化一旦进历史就会持续爆炸，必须原样搬 | `gw-kiro/converter` | P1 | high |
| 空 content 400 历史 assistant 兜底 | `src/anthropic/converter.rs:1385-1469 (convert_assistant_message)`; `src/anthropic/converter.rs:1471-1515 (merge_assistant_messages)` | 历史 assistant 消息转译与合并，包含“thinking 丢弃后若正文/tool_use 皆空则补占位符” | 这是处理上游空响应残留和 thinking 历史裁剪后的必要防毒层；不搬会重新引入“历史空 assistant 毒化全会话” | `gw-kiro/converter` | P1 | high |
| ConversationState 核心转换 | `src/anthropic/converter.rs:475-687 (convert_request)` | Anthropic Messages → Kiro `ConversationState` 主流程：模型映射、prefill 截断、conversationId、history/currentMessage、tool pairing、空内容兜底、cachePoint 实验位 | 这是整个 Kiro 请求构造的主装配器，里面堆了大量协议差异与线上补丁，重写成本最高、回归面最广 | `gw-kiro/converter` | P1 | high |
| message content 归一化抽取 | `src/anthropic/converter.rs:696-764 (process_message_content)`; `src/anthropic/converter.rs:791-807 (anthropic_document_to_kiro)`; `src/anthropic/converter.rs:827-853 (anthropic_image_to_kiro)`; `src/anthropic/converter.rs:857-893 (extract_images_from_tool_result_content, extract_tool_result_content)` | 把 Anthropic content blocks 抽成文本、图片、文档、tool_result，兼容 base64 image/document 和 tool_result 内截图 | 这部分定义了内部 IR 萃取口径，后续 converter/cache_sim 都建立在此；尤其媒体/截图/tool_result 的拆法已经过线上验证 | `gw-kiro/converter` | P1 | high |
| 历史构建与 system 折叠 | `src/anthropic/converter.rs:1207-1323 (build_history)` | 把 system/thinking 前缀/structured output 指令折进 history，按 user/assistant 对组装 Kiro 历史；最后一条 user 留作 current | Kiro 没独立 system 字段，这里定义了历史与 current 的真实落位，直接影响 prefix cache、thinking、结构化输出和身份锚点 | `gw-kiro/converter` | P1 | high |
| thinking/system/output 指令注入 | `src/anthropic/converter.rs:1163-1205 (generate_thinking_prefix, has_thinking_tags, structured_output_instruction)` | 生成 thinking mode 前缀与结构化输出 system 指令，并在 `build_history` 中按规则注入 | 这部分和 Kiro 上游 prompt 语义高度绑定，随便改会影响 thinking 启动、结构化输出和缓存稳定性 | `gw-kiro/converter` | P2 | high |
| conversationId 稳定派生 | `src/anthropic/converter.rs:353-399 (derive_conversation_id_from_messages)`; `src/anthropic/converter.rs:305-316 (normalized_client_system)`; `src/anthropic/converter.rs:286-297 (strip_rolling_fingerprints)`; `src/anthropic/converter.rs:320-329 (safe_prefix)` | v55 conversationId 机制：以前两条 user + system anchor 哈希稳定派生，并剥掉会滚动的 `x-anthropic-*` 指纹行 | 这是 prefix cache 命中率修复的头号资产；算法参数、anchor 长度和系统归一化口径都来自真实线上分析，不应重拍脑袋 | `gw-kiro/converter` 或 `gw-kiro/cache_sim` | P1 | high |
| agentContinuationId 稳定派生 | `src/anthropic/converter.rs:408-426 (derive_agent_continuation_id)` | 基于 conversationId 再派生稳定 agentContinuationId，避免同会话每轮新 UUID 导致后端 cache miss | 真实 Kiro 后端不仅看 conversationId，还看 continuation id；不搬这层，conversationId 修好了也可能继续低命中 | `gw-kiro/converter` | P1 | high |
| tool pairing 清理链 | `src/anthropic/converter.rs:907-977 (validate_tool_pairing)`; `src/anthropic/converter.rs:988-1014 (remove_orphaned_tool_uses)`; `src/anthropic/converter.rs:1023-1073 (remove_orphaned_tool_results)` | 清理历史/current 中孤立的 tool_use 和 tool_result，避免 Kiro 因工具配对不完整返回 400；删除 tool_result 后若 user 变空再补占位符 | Claude Code auto-compact 会留下各种孤儿工具块，这部分是非常真实的会话自愈逻辑；不搬会反复中 400 “Improperly formed request” | `gw-kiro/converter` | P1 | high |
| 工具定义缩短与 schema 规范化 | `src/anthropic/converter.rs:98-141 (normalize_json_schema)`; `src/anthropic/converter.rs:447-471 (request_has_chunked_tools, create_placeholder_tool)`; `src/anthropic/converter.rs:1075-1161 (shorten_tool_name, map_tool_name, convert_tools)` | 修正 MCP 工具 schema 里的 null/脏字段、给超长工具名做哈希缩短、为历史缺失工具补 placeholder definition | 这是 Claude Code/MCP 大工具集能稳定过 Kiro 的必要兼容层；重写常会忽略超长名和 schema 脏数据，导致难解 400 | `gw-kiro/converter` | P2 | high |
| cache_sim 前缀命中纯函数 | `src/kiro/cache_sim.rs:95-104 (prefix_cache_read)` | 比较上一轮/本轮指纹序列的最长公共前缀，输出命中的 token 总数 | 这是 v53 统一模拟器的最小核心，但其定义已经和 Kiro/Anthropic 的 prefix 语义对齐，直接搬最稳 | `gw-kiro/cache_sim` | P1 | high |
| cache_sim 状态表与 observe | `src/kiro/cache_sim.rs:127-223 (CacheSimStore::{new,set_ttl_secs,set_max_sessions,observe_at})`; `src/kiro/cache_sim.rs:227-250 (evict, global, observe)` | 带 TTL/LRU 的会话状态表，维护上一轮指纹、模型和过期淘汰，是统一模拟器的有状态部分 | 命中模拟不只是纯函数，还要有进程内跨轮状态；TTL/LRU/热调参数都是运营可调资产，重写容易口径飘 | `gw-kiro/cache_sim` | P1 | high |
| cache_sim 从 ConversationState 提取稳定指纹 | `src/kiro/cache_sim.rs:271-301 (fingerprints_from_state)`; `src/kiro/cache_sim.rs:325-370 (push_chunk_fingerprints, chunk_hash)`; `src/kiro/cache_sim.rs:375-428 (canon_user, canon_assistant)` | 从处理后的 ConversationState 提取 chunk 级稳定语义指纹，忽略 tools 列表和 current/history 容器差异，解决 v53 指纹崩盘 | 这是模拟器最难的部分：不是“hash 一下文本”那么简单，而是把跨轮位置、chunk、tool_use/tool_result 都校准过的生产算法 | `gw-kiro/cache_sim` | P1 | high |
| cache_read 统一上报公式 | `src/anthropic/usage.rs:55-87 (reported_cache_read)`; `src/anthropic/usage.rs:100-122 (build_usage_json)` | v53 统一计费公式：用模拟器 `hit/sim_total` 比例映射到权威 `report_total`，乘倍率并用 floor/cap 夹限，再生成 Anthropic usage JSON | 这是对外计费口径，不只是内部估算；公式、边界和双计费修复都已线上验证，重写风险直接体现在用户账单上 | `gw-kiro/billing` | P1 | high |
| 流式 usage/cache_sim 接线 | `src/anthropic/stream.rs:691-709 (set_cache_read_multiplier, set_cache_cap_ratio, set_cache_floor_ratio, set_sim_cache)`; `src/anthropic/stream.rs:509-523 (SseStateManager::generate_final_events usage)` | 把 cache_sim 结果与运营倍率参数接进流式 Anthropic usage 输出 | 即便公式有了，若流式路径接线错，最终用户看到的 usage 仍会和非流式不一致；这块是 v53 “统一模拟器”闭环的一部分 | `gw-kiro/stream` + `gw-kiro/billing` | P2 | high |
| 多账号会话亲和调度 | `src/kiro/token_manager.rs:816-913 (select_by_session_affinity)`; `src/kiro/token_manager.rs:1000-1211 (acquire_context_with_session, acquire_context_with_session_and_group)` | v52 “落号即认”调度器：新会话按优先级分层 LRU 选 primary；primary 不可用时立即转正到新号，不再回弹；并支持 allowed_group 隔离与并发 permit | 这是解决缓存橡皮筋横跳的核心资产，策略细节都是线上命中率换来的；随意重写会很快回到跨号抖动老问题 | `gw-kiro/scheduler` | P1 | high |
| 通用选号器 priority/balanced | `src/kiro/token_manager.rs:920-979 (select_next_credential)` | 非 affinity 情况的选号策略：priority 模式按优先级，balanced 模式按 success_count 最少 + priority 决策 | 这是多账号基本调度语义，后续 affinity fallback、admin 面板、分组隔离都建立在它之上 | `gw-kiro/scheduler` | P2 | high |
| token_manager 数据结构与禁用分类 | `src/kiro/token_manager.rs:409-446 (AffinityEntry, CredentialEntry)`; `src/kiro/token_manager.rs:450-469 (DisabledReason)` | 管理多账号调度所需的完整运行时状态：success_count、last_selected_at、empty 窗口、并发 semaphore、禁用原因分类等 | 这些字段就是调度算法的真实状态机；如果只“抄逻辑不抄状态”，后面很多分支根本无法对齐原行为 | `gw-kiro/scheduler` | P1 | high |
| 空响应阈值冷却 | `src/kiro/token_manager.rs:1690-1759 (report_empty_response)` | v58 空响应健康机制：固定窗口累计 empty，达阈值才冷却账号；未达阈值只计数不切号 | 这是 empty-response 从“每次都伤号”改成“只边缘化坏号”的关键资产，必须与 fallback 配套搬，不然会重新误杀健康号 | `gw-kiro/scheduler` | P1 | high |
| 429 冷却与自动自愈 | `src/kiro/token_manager.rs:1633-1677 (report_rate_limited)`; `src/kiro/token_manager.rs:1039-1057 (cooldown sweep in acquire_context_with_session_and_group)` | 429 命中后临时禁用、冷却到期后选号前自动自愈恢复 | 这是多账号在真实上游限流下还能平稳切号/恢复的关键运维逻辑，不能只保留“报错”不保留“恢复” | `gw-kiro/scheduler` | P2 | high |
| token/refresh 失败分类与自愈 | `src/kiro/token_manager.rs:1765-1819 (report_refresh_failure)`; `src/kiro/token_manager.rs:1103-1121 (all-disabled self-heal branch)` | 刷新失败累计、全账号自动禁用后的自愈重启语义 | 虽然不是本次 11 项主需求之一，但它和多账号调度是同一资产面；新网关若只搬选号不搬失败恢复，线上稳定性会差很多 | `gw-kiro/scheduler` | P3 | medium |
| machine_id 派生与兜底缓存 | `src/kiro/machine_id.rs:24-43 (normalize_machine_id)`; `src/kiro/machine_id.rs:54-86 (generate_from_credentials)`; `src/kiro/machine_id.rs:94-109 (fallback_machine_id)` | 机器指纹生成：优先凭据级/全局配置，其次按 API Key 或 refreshToken 派生，最后按 credential.id 进程内稳定兜底 | Kiro 的 machine_id 不只是随机串，和认证链路、同凭据稳定性有关；旧实现已经把多种凭据形态都踩顺了 | `gw-kiro/machine_id` | P1 | high |
| provider 对 machine_id / scheduler / 计费参数的接线 | `src/kiro/provider.rs:151-168 (cache_read_multiplier, report_empty_response)`; `src/kiro/provider.rs:258-285 (non-stream request machine_id header path)`; `src/kiro/provider.rs:429-455 (stream request machine_id header path)` | KiroProvider 层把 token_manager、machine_id、请求 header 和空响应健康机制连起来 | 虽然你这次主要盘“旧资产”，但若只搬子模块不搬 provider 接线，很多能力在新网关里不会真的生效 | `gw-kiro`（provider/egress） | P2 | medium |
| Kiro 事件到高层语义的桥 | `src/anthropic/handlers.rs:1024-1111 (parse_buffered_response event match)`; `src/anthropic/stream.rs:666-670 (Event::from_frame + process_kiro_event call site)` | 旧系统对 `AssistantResponse/ReasoningContent/ToolUse/ContextUsage/Metering/TokenUsage/Exception` 的高层解释口径 | 这决定“什么事件算内容、什么事件影响 stop_reason 和 usage”，是 parser 之后最值钱的一层语义资产 | `gw-kiro/parser` + `gw-kiro/stream` + `gw-kiro/billing` | P1 | medium |
