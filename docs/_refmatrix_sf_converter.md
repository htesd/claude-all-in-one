# static_flow converter 簇 — 实现参考

| 能力点 | 参考来源(文件:行号 函数名) | 做什么 | 对claude-all-in-one价值 | 落到claude-all-in-one模块 | 建议Phase | confidence |
|---|---|---|---|---|---|---|
| 顶层转换总管线 | converter/convert.rs:147-221 (convert_normalized_request_with_resolved_session) | 串起 model 映射、严格校验、current/history 切分、thinking 注入、tool 转换、文档去重、tool 配对、placeholder tool 补齐、multimodal schema 兼容，最终组装 Kiro `ConversationState` | 这是新项目 `gw-kiro` 的主参考骨架，能直接定义 converter 主流程职责边界 | gw-kiro/converter/convert | P1单worker直通 | high |
| 当前 user turn 范围锚定 | converter/convert.rs:42-64 (trailing_user_message_start, current_user_message_range) | 从消息尾部回溯最后一段连续 `user` turns，确定 current turn range，而不是只看最后一条 | 对 Anthropic Messages IR 很重要，避免把同轮多 user 消息拆错，影响缓存前缀稳定和 tool_result 归属 | gw-kiro/converter/session | P1单worker直通 | high |
| 无 user 请求硬拒绝 | converter/convert.rs:66-72 (no_user_message_error) | 对空 messages 或 assistant-prefill-only 请求给出明确 invalid_request | 新网关需要尽早拒绝不合法 IR，减少后续状态机分支 | gw-kiro/converter/validate | P1单worker直通 | high |
| web_search_tool_result 文本化回放 | converter/convert.rs:74-112 (web_search_tool_result_text) | 把 `web_search_tool_result` 的结构化内容转成可放回 history 的普通文本，兼容 `encrypted_content/snippet` | 若 claude-all-in-one 也做 server tool/websearch 短路，这个函数能定义历史回放格式 | gw-kiro/converter/websearch | P2流式+资产 | high |
| 多模态消息内容提取 | converter/convert.rs:226-297 (process_message_content) | 统一从 string/blocks 中抽取 text、images、documents、tool_results，并把 tool_result 中嵌的图片/文档也提升出来 | 这是 converter 报文簇的核心复用点，可避免 current/history 各写一套解析逻辑 | gw-kiro/converter/convert | P1单worker直通 | high |
| tool_result 附件占位兜底 | converter/convert.rs:260-281 (process_message_content) | 当 tool_result 文本为空但携带图片/文档时，写入 `EMPTY_TOOL_RESULT_PLACEHOLDER`，避免上游拒绝空结果 | 对工具产出多模态但无文本的场景是关键兼容层 | gw-kiro/converter/tool_result | P2流式+资产 | high |
| history 注入 synthetic system 对话对 | converter/convert.rs:303-317 (build_history) | 把顶层 system 拼成一组 synthetic `user/assistant` history 前缀：先注入 system，再加 “I will follow these instructions.” | 是把 Anthropic top-level system 投影到 Kiro 历史的关键做法，可直接参考 | gw-kiro/converter/system | P1单worker直通 | high |
| 同角色连续消息 merge | converter/convert.rs:321-348 (build_history) | 将 history 中连续 user 或 assistant 消息合并为单 turn，减少碎片并贴合 Kiro 会话结构 | 有利于 conversation prefix 稳定，也降低 history 噪音 | gw-kiro/converter/convert | P1单worker直通 | high |
| current user 多消息合并 | converter/convert.rs:351-386 (merge_current_user_messages) | 聚合同轮多个 user 消息，拼接文本、汇总 images/documents/tool_results，并限制当前消息图片上限 | 直接对应 claude-all-in-one 当前输入装配逻辑 | gw-kiro/converter/convert | P2流式+资产 | high |
| history user 多消息合并 | converter/convert.rs:388-422 (merge_user_messages) | 为历史 user turn 聚合内容、附件、tool_results，生成 `HistoryUserMessage` | 新项目也需要 history/current 分别落线，职责很清晰 | gw-kiro/converter/convert | P1单worker直通 | high |
| 会话级文档去重与总量限制 | converter/convert.rs:424-455 (dedupe_documents_in_place, dedupe_history_and_current_documents, ensure_document_content_placeholder) | 按 document name 去重历史+当前文档；超上限直接报错；若仅有文档无文本则补占位文本 | 文档类请求极易踩 Kiro 空文本/重复附件坑，这套逻辑值得完整迁移思路 | gw-kiro/converter/document | P2流式+资产 | high |
| assistant thinking/text/tool_use 合并 | converter/convert.rs:464-558 (convert_assistant_message, merge_assistant_messages) | 将 assistant blocks 转成 Kiro assistant history：thinking 包成 `<thinking>...</thinking>`、tool_use 名称映射、纯 tool_use 用单空格占位 | 对 history 重放很重要，尤其是纯 tool_use / thinking-only turn 的兼容 | gw-kiro/converter/convert | P2流式+资产 | high |
| 保守归一化总入口 | converter/normalize.rs:293-502 (normalize_request) | 先做窄范围 cleanup：处理 system/developer 角色、删除尾随消息、删空白块、文档块规范化、tool 规范化、duplicate tool_use_id rewrite | 这是 claude-all-in-one 在“严格校验前的安全预处理层”最佳参考 | gw-kiro/converter/normalize | P1单worker直通 | high |
| 归一化事件记录 | converter/normalize.rs:17-34 (push_normalization_event) | 以结构化 event 记录 message/block 级 rewrite/drop/promote 原因 | claude-all-in-one 若要做 admin/debug 面板，这是极高价值的可观测性切口 | gw-kiro/converter/normalize | P4存储admin | high |
| 单条消息清洗器 | converter/normalize.rs:36-209 (normalize_message) | 删除空白 string/text/thinking；user document 走专门规范化；assistant 的 `server_tool_use` 删除；`web_search_tool_result`、assistant `tool_result` 改写成 text | 覆盖大量 transport noise 和 Kiro 不可表示块，属于“宁窄勿错”的预处理模板 | gw-kiro/converter/normalize | P1单worker直通 | high |
| system 三级分流总判定 | converter/normalize.rs:211-248 (system_role_disposition) | 把 `system` role 分成三类：稳定前缀直接 promote 到 top-level system；动态噪音 drop；其他内容 preserve 为 in-order user context | 正中你关注的 system promote/drop/preserve，建议作为 claude-all-in-one 明确子模块实现 | gw-kiro/converter/system | P1单worker直通 | high |
| 稳定 system 前缀识别 | converter/normalize.rs:250-257 (is_stable_system_prefix_message) | 识别 `SessionStart hook additional context` 与 Claude Code/Agent SDK 标识行，作为稳定 system prefix | 有助于缓存稳定，避免把 CLI 壳层身份串错放进会话 history | gw-kiro/converter/system | P1单worker直通 | high |
| interrupted user message 保序保留 | converter/normalize.rs:259-268 (interrupted_user_message_payload) | 特判 “The user sent a new message while you were working:” system 文本，提取真实 user payload 后按 user context 保留 | 对 Claude Code 中断续聊兼容非常有价值，可减少身份/系统信息污染 | gw-kiro/converter/system | P2流式+资产 | high |
| 动态 system 噪音识别 | converter/normalize.rs:270-273 (is_dynamic_system_noise) | 识别诸如 “The task tools haven't been used recently.” 这类动态 system 文本并丢弃 | 避免 cache prefix 被 session-local 噪音抖动污染 | gw-kiro/converter/system | P1单worker直通 | high |
| developer role 提升为 top-level system | converter/normalize.rs:357-385 (normalize_request) | OpenAI 风格 `developer` 角色直接 promote 到 Anthropic `system` 字段，而不是当普通对话消息保留 | 对多客户端归一化非常关键，能统一 OpenAI/Anthropic 风格入口 | gw-kiro/converter/normalize | P1单worker直通 | high |
| 尾随非 user turn 剪裁 | converter/normalize.rs:393-477 (normalize_request) | 删除 last user 之后的所有消息，以及 empty user noop 后紧跟的 assistant，保证 history 截到最后保留 user | 能防止无效尾巴污染 conversion 和 conversation state | gw-kiro/converter/normalize | P1单worker直通 | high |
| tool 归一化与 duplicate id rewrite 串接 | converter/normalize.rs:479-502 (normalize_request) | 在 request 归一化尾部统一做 tool 规范化、收集 tool 校验摘要、重写重复 tool_use_id | 说明 normalize 阶段就能完成一大半 tool 清洗，而不是放到下游解析期 | gw-kiro/converter/tools | P1单worker直通 | high |
| 顶层请求硬校验入口 | converter/validate.rs:5-18 (validate_messages_request) | 限定只接受 `user/assistant` 角色进入严格校验，并分角色校验内容 | 是 claude-all-in-one validator 层的直接参考入口 | gw-kiro/converter/validate | P1单worker直通 | high |
| user 内容硬校验 | converter/validate.rs:20-153 (validate_user_message_content) | 校验 user string/array 非空；text/image/document/tool_result 各自必填字段完整；拒绝未知 block type | 这是“报文合法化硬校验”的最主要参考 | gw-kiro/converter/validate | P1单worker直通 | high |
| assistant 内容硬校验 | converter/validate.rs:155-256 (validate_assistant_message_content) | 校验 assistant string/array 非空；允许 text/thinking/tool_use；tool_use 必须有 id/name；拒绝未知块 | 可直接形成 claude-all-in-one 的 assistant history 输入门槛 | gw-kiro/converter/validate | P1单worker直通 | high |
| role-message 转 system 文本 | converter/system.rs:14-58 (system_message_from_role_message) | 把 `system/developer` 角色消息统一解出文本，只允许 string 或纯 text blocks，其他类型报错 | 支撑上层 system promote/preserve 流程，也是多客户端归一化关键基础件 | gw-kiro/converter/system | P1单worker直通 | high |
| system 文本清洗 billing header | converter/system.rs:60-63 (cleaned_system_message_text) | 在 system 文本进入稳定前缀判定前先去掉 volatile billing header，并过滤空文本 | 与身份防穿帮强关联，可防系统头抖动影响缓存和身份探针 | gw-kiro/converter/system | P1单worker直通 | high |
| 注入系统内容构建器 | converter/system.rs:65-92 (build_injected_system_content) | 汇总 top-level system、清 billing header、模型身份归一化、chunked policy、identity override、structured output 指令，形成最终 injected system | 是 static_flow 最有信息密度的 system 组装点，claude-all-in-one 必须重点参考 | gw-kiro/converter/system | P1单worker直通 | high |
| conversationId 解析总入口 | converter/session.rs:65-131 (resolve_conversation_id_from_metadata) | 从 metadata.user_id 中优先解析 JSON `session_id`，其次解析 legacy `session_<uuid>`，失败则生成 fallback UUID 并记录来源 | 对你关注的 conversationId 会话锚定是直接命中能力点 | gw-kiro/converter/session | P1单worker直通 | high |
| session source preview 截断 | converter/session.rs:11-21 (preview_session_value) | 把原始 session 来源值裁剪成有限预览文本，便于日志/管理面板展示 | admin 可观测性细节值得带上 | gw-kiro/converter/session | P4存储admin | high |
| UUID 合法性快速判定 | converter/session.rs:46-48 (is_valid_uuid) | 用轻量规则判断 UUID 形态，避免引入更重解析分支 | 可作为会话锚定前置校验小工具 | gw-kiro/converter/session | P1单worker直通 | high |
| tool 定义预归一化 | converter/tools.rs:26-66 (normalize_tools) | 对客户端传入 tools 做前置清洗：空名字直接拒绝、空描述补默认值、收集 schema 关键词统计 | 是多客户端输入归一化与 admin 诊断的有用参考 | gw-kiro/converter/tools | P1单worker直通 | high |
| tool 描述补默认值 | converter/tools.rs:18-24 (normalize_tool_description) | 对空 description 填 `Client-provided tool '<name>'` | 简单但实用，可减少 provider 对空描述的兼容坑 | gw-kiro/converter/tools | P1单worker直通 | high |
| tool 转 Kiro spec | converter/tools.rs:71-106 (convert_tools) | 做 tool name 映射、Write/Edit 描述追加政策后缀、description 截断 10K、schema JSON 正规化 | 新项目 provider 实现里几乎可直接照抄结构 | gw-kiro/converter/tools | P1单worker直通 | high |
| structured output synthetic tool 注入 | converter/tools.rs:108-154 (extract_structured_output_schema, make_structured_output_tool_name, structured_output_instruction, append_structured_output_tool) | 从 `output_config.json_schema` 提取 schema，注入 synthetic tool，并生成“最终答案必须以该 tool 调用返回”的 system 指令 | 若 claude-all-in-one 也支持 Anthropic structured output，这是非常关键的桥接设计 | gw-kiro/converter/tools | P2流式+资产 | high |
| orphaned tool_result 当前轮验证 | converter/tool_pairing.rs:10-50 (validate_tool_pairing) | 统计 history 里已有 tool_use/tool_result，当前轮只保留能补齐 pending tool_use 的 tool_results，并返回 orphaned tool_use ids | 这正是你关注的 tool 配对核心逻辑之一 | gw-kiro/converter/tool_pairing | P1单worker直通 | high |
| orphaned tool_use 清理 | converter/tool_pairing.rs:52-68 (remove_orphaned_tool_uses) | 从 history assistant tool_uses 中删除没有对应 tool_result 的遗留调用 | 能减少 Kiro 对不完整工具对话的拒绝 | gw-kiro/converter/tool_pairing | P1单worker直通 | high |
| orphaned history tool_results 修剪 | converter/tool_pairing.rs:70-108 (prune_orphaned_history_tool_results) | 顺序扫描 history，只保留能匹配到先前 assistant tool_use 的 tool_results，并删除因此变空的 user turn | 正中你点名的 orphan prune，建议单独子模块实现 | gw-kiro/converter/tool_pairing | P1单worker直通 | high |
| duplicate tool_use_id 重写总逻辑 | converter/tool_name.rs:13-130 (rewrite_duplicate_tool_use_ids) | 跟踪 active tool_use，遇到重复 id 时改写 assistant `tool_use.id` 与后续 user `tool_result.tool_use_id`；若冲突时机非法则直接报错 | 正中你点名的 duplicate `tool_use_id` rewrite，是值得单列能力点的核心逻辑 | gw-kiro/converter/tool_pairing | P1单worker直通 | high |
| 现存 tool_use_id 收集 | converter/tool_name.rs:132-156 (collect_existing_tool_use_ids) | 扫描 request 中已有 tool_use id，为 rewrite 去重提供基线集合 | 属于 duplicate rewrite 的配套基础件 | gw-kiro/converter/tool_pairing | P1单worker直通 | high |
| tool_use_id Kiro-safe 规范化 | converter/tool_name.rs:158-211 (next_rewritten_tool_use_id, normalize_tool_use_id_for_kiro, is_valid_kiro_tool_use_id, sanitize_tool_use_id) | 将非法字符替换为 `_`，必要时拼接 `__sfdupN` 后缀，保证 tool_use_id 可被 Kiro 接受且全局唯一 | 能减少不同客户端生成奇怪 tool ids 时的兼容问题 | gw-kiro/converter/tool_pairing | P1单worker直通 | high |
| history tool 名收集 + placeholder tool | converter/tool_name.rs:215-241 (collect_history_tool_names, create_placeholder_tool) | 收集 history assistant 中用过的 tool 名；若当前请求未携带这些 tool，就补 permissive placeholder tool | 是“历史里有 tool_use 但当前 tool 集不全”场景的关键兼容层 | gw-kiro/converter/tools | P2流式+资产 | high |
| tool 名 sanitize | converter/tool_name.rs:243-273 (truncate_tool_name_prefix, sanitize_tool_name) | 非法字符归一为 `_`，去掉连续分隔符，空名回退 `tool` | 正中你点名的 tool naming sanitize | gw-kiro/converter/tool_name | P1单worker直通 | high |
| tool 名 hash alias | converter/tool_name.rs:276-304 (make_hashed_tool_name_alias, map_tool_name) | 超长或非法 tool name 生成 `visiblePrefix_hash8` 形式的稳定别名，并维护 alias→original 反向映射 | 正中你点名的 hash alias；也是多客户端 tool 兼容的高 ROI 逻辑 | gw-kiro/converter/tool_name | P1单worker直通 | high |
| tool_result 附件提取 | converter/tool_result.rs:11-80 (extract_tool_result_attachments, extract_tool_result_attachments_from_value) | 从 tool_result.content 中提取 image/document 附件，支持数组、单对象、字符串化 JSON 三种形态 | 对工具返回多模态资产的链路很关键 | gw-kiro/converter/tool_result | P2流式+资产 | high |
| tool_result 文本提取 | converter/tool_result.rs:82-107 (extract_tool_result_content, extract_tool_result_content_from_value) | 统一从字符串、block 数组、单对象提取文本内容，兜底序列化其他 JSON | 是 tool_result 文本标准化的主入口 | gw-kiro/converter/tool_result | P1单worker直通 | high |
| stringified tool_result JSON 识别 | converter/tool_result.rs:109-138 (parse_stringified_tool_result_content, looks_like_tool_result_content_block) | 识别被包成字符串的 JSON block 内容，并仅对看起来像 text/image/document block 的 payload 解包 | 对多客户端兼容尤其有用，避免误把任意 JSON 文本当结构化块 | gw-kiro/converter/tool_result | P2流式+资产 | high |
| permissive object schema | converter/schema.rs:9-16 (permissive_object_schema) | 构造最宽松 object schema，作为 schema 坏掉或 multimodal 不兼容时的回退 | 适合做 provider 侧安全降级策略 | gw-kiro/converter/schema | P2流式+资产 | high |
| schema 顶层字段修复 | converter/schema.rs:21-55 (normalize_json_schema) | 确保 schema 至少有 `type/properties/required/additionalProperties`，并修复 null/错型字段 | 正中你点名的 schema JSON 归一化去抖 | gw-kiro/converter/schema | P1单worker直通 | high |
| schema 关键词统计 | converter/schema.rs:57-74 (collect_schema_keywords) | 递归统计 schema 中出现的“不兼容关键词”次数，用于诊断 | 对 admin 面板与问题排查很有价值 | gw-kiro/converter/schema | P4存储admin | high |
| multimodal schema 兼容降级 | converter/schema.rs:76-121 (schema_contains_multimodal_unsupported_keywords, content_value_contains_image, request_message_contains_image, apply_multimodal_tool_schema_compatibility) | 当请求携带图片时，若 tool schema 含 Kiro 不支持关键词，就把 input_schema 降成 permissive object | 是“图片+tools 共存”场景的关键稳态兼容策略 | gw-kiro/converter/schema | P2流式+资产 | high |
| 有图请求探测 | converter/schema.rs:89-109 (content_value_contains_image, request_message_contains_image) | 递归识别 message 或 tool_result content 中是否含 image block | schema 降级、资产路由、计费/限流都可能复用这个探针 | gw-kiro/converter/image | P2流式+资产 | high |
| 有效 response identity 推导 | converter/identity.rs:246-270 (effective_response_identity_for_request) | 从 system 文本里推断平台身份和模型身份；若没有，则回退到请求 model；并可提取 repo name hint | 是身份防穿帮的总入口，建议新项目也单独成模块 | gw-kiro/converter/identity | P1单worker直通 | high |
| 当前轮身份探针分型 | converter/identity.rs:272-301 (response_identity_for_current_turn) | 根据当前 user 内容判定是 model-only、多重身份、冲突探针等不同身份回复策略 | 若 claude-all-in-one 要过身份探针，这是最直接的规则参考 | gw-kiro/converter/identity | P2流式+资产 | high |
| identity override 注入 | converter/identity.rs:303-316 (anthropic_identity_override) | 生成 `<identity_override>` system 指令，强制回答自己是 Claude/Anthropic API，不承认 Kiro/Warp 等产品身份 | 正中你点名的 identity override | gw-kiro/converter/identity | P1单worker直通 | high |
| Claude Code 模型身份重写 | converter/identity.rs:318-358 (normalize_claude_code_model_identity) | 把 system 文本中已有或缺失的 “You are powered by the model named ...” 行统一改成期望模型身份 | 对多客户端归一化和身份探针一致性都很重要 | gw-kiro/converter/identity | P1单worker直通 | high |
| 清除 billing header | converter/identity.rs:360-374 (strip_volatile_claude_code_billing_header, is_claude_code_billing_header_text) | 去除 `x-anthropic-billing-header` 一类 volatile 系统头文本 | 正中你点名的 “清 billing header” | gw-kiro/converter/identity | P1单worker直通 | high |
| 身份探针识别规则库 | converter/identity.rs:126-233 (is_multi_identity_probe_zh/en, is_conflict_probe_zh/en, has_platform_identity_intent_zh/en, mentions_conflict_product, contains_ascii_token, is_model_identity_probe, model_identity_probe_language) | 一组识别“你是谁/什么模型/多重身份/平台冲突”问题的启发式函数 | 即使不原样照搬，也很值得作为 claude-all-in-one 身份安全策略样本库 | gw-kiro/converter/identity | P2流式+资产 | high |
| thinking 前缀生成 | converter/thinking.rs:15-37 (generate_thinking_prefix) | 将 Anthropic `thinking.enabled/adaptive` 配置转成 Kiro 侧 XML 前缀，adaptive 保留精确 effort label | 是 Anthropic IR → Kiro thinking 参数桥接的核心 | gw-kiro/converter/thinking | P2流式+资产 | high |
| 只对当前 turn 注入 thinking | converter/thinking.rs:39-60 (has_thinking_tags, apply_thinking_prefix_to_current_turn) | 若当前 user 内容还没带 thinking tags，则仅在 current turn 前缀注入，避免污染 history cache prefix | 很符合你要的新架构里“IR 稳定，当前轮动态参数外插”的思路 | gw-kiro/converter/thinking | P2流式+资产 | high |
| 图片格式识别 | converter/image.rs:5-55 (get_image_format_from_source, detect_image_format_from_base64, detect_image_format_from_bytes, get_image_format) | 优先从 base64 前缀和 magic bytes 识别 jpeg/png/gif/webp，其次看 media_type | 正中你点名的图片处理，且比只信 media_type 更稳 | gw-kiro/converter/image | P2流式+资产 | high |
| document block 归一化入口 | converter/document.rs:11-30 (normalize_user_document_block) | user document block 先走 payload 规范化，再在发生 rewrite 时记录 normalization event | 适合作为 claude-all-in-one document normalize 子模块入口 | gw-kiro/converter/document | P2流式+资产 | high |
| document payload 硬校验 + 规范化 | converter/document.rs:32-111 (normalize_document_block_payload) | 校验 source/type/media_type/data 必填；仅允许 `base64` 或部分文本类的 `text` source；统一媒体类型、换行、文件名 | 正中你点名的文档处理，也兼具报文合法化作用 | gw-kiro/converter/document | P2流式+资产 | high |
| document media type canonicalize | converter/document.rs:113-149 (canonical_document_media_type, document_media_type_supports_text_source, document_format_from_media_type) | 统一接受的 document mime 集合及其格式映射，决定哪些媒体类型允许 text source | 新项目需要明确 allowlist 时非常适合参考 | gw-kiro/converter/document | P2流式+资产 | high |
| document 名 sanitize/生成 | converter/document.rs:151-203 (normalize_document_name, sanitize_document_name, generate_document_name) | 用户给了 name 就净化 stem；没给则按 media_type+data hash 生成稳定名字 | 这能直接支撑文档去重与缓存稳定 | gw-kiro/converter/document | P2流式+资产 | high |
| document 转 Kiro 附件 | converter/document.rs:205-244 (kiro_document_from_source, kiro_document_from_block) | 把 Anthropic document source 规范化后转成 Kiro `KiroDocument`，text source 会先转 base64 | 是 document 进入 provider wire 的直接桥接点 | gw-kiro/converter/document | P2流式+资产 | high |
| model 名映射 | converter/model.rs:5-28 (map_model) | 把 Anthropic 模型别名/日期版名解析到 Kiro 支持的 canonical model id | 是 provider 最基本的模型桥接能力 | gw-kiro/converter/model | P1单worker直通 | high |
| context window 推导 | converter/model.rs:30-43 (get_context_window_size) | 根据映射后的 model 判定 1M 或 200K context window | 若 claude-all-in-one 后续做 cache_sim / truncation / scheduler，也可直接复用思路 | gw-kiro/converter/model | P2流式+资产 | high |
| 受保护内容总校验 | anthropic/protected_content.rs:31-41 (validate_protected_content) | 在 request 进入下游前，逐消息校验 protected thinking 与 encrypted content 规则 | 正中你点名的受保护内容处理，建议在 provider 入口单独跑一道 | gw-kiro/converter/protected_content | P2流式+资产 | high |
| thinking 签名严格校验 | anthropic/protected_content.rs:43-95 (validate_message) | 仅允许 assistant thinking block 携带 signature；必须有 thinking+signature；并验证签名真实性 | 若 claude-all-in-one 需要保真透传 thinking，这是非常关键的安全闸门 | gw-kiro/converter/protected_content | P2流式+资产 | high |
| 拒绝未签名 thinking 标签 | anthropic/protected_content.rs:97-153 (reject_raw_assistant_thinking_tags, reject_raw_thinking_text, reject_raw_thinking_tags_in_normalized_assistant_block) | 扫描 assistant 字符串/text block/web_search/tool_result 文本，发现真实 `<thinking>` 标签就拒绝 | 能防止伪造 thinking 或 history 注入绕过 | gw-kiro/converter/protected_content | P2流式+资产 | high |
| encrypted_content allowlist | anthropic/protected_content.rs:155-198 (reject_encrypted_content_for_message, reject_encrypted_content) | 默认拒绝所有不可验证 `encrypted_content`，只对 gateway 自己生成的 web_search_result 放行 | 是受保护内容与 websearch 交叉点的关键策略 | gw-kiro/converter/protected_content | P2流式+资产 | high |
| websearch 工具识别与短路路由 | anthropic/websearch.rs:73-99 (has_web_search_tool, should_route_mcp_web_search, remove_web_search_tools) | 仅当工具集是纯 `web_search`、最新消息为 user，且 query 前缀或 forced tool_choice 命中时，走 MCP websearch；并可从 request 中剥离该工具 | 正中你点名的 websearch 处理主入口 | gw-kiro/converter/websearch | P2流式+资产 | high |
| websearch query 提取 | anthropic/websearch.rs:101-165 (extract_search_query, latest_message_is_user, latest_user_message_text, message_text, has_prefixed_search_query, tool_choice_forces_web_search) | 从 latest user turn 里提取 prefixed query 或 forced tool request，并避免误取旧 turn | 新项目若支持 websearch 旁路，query 解析逻辑可直接参考 | gw-kiro/converter/websearch | P2流式+资产 | high |
| 生成 MCP websearch 请求 | anthropic/websearch.rs:167-185 (create_mcp_request) | 构造 JSON-RPC `tools/call` 请求，并生成 `srvtoolu_*` 风格 tool_use_id | websearch server tool 化时可沿用这一接口层形状 | gw-kiro/converter/websearch | P2流式+资产 | high |
| 解析 websearch 结果 | anthropic/websearch.rs:187-197 (parse_search_results) | 从 MCP 返回中仅接受 `result.content[0].type=text` 的 JSON 结果并反序列化 | 提供了服务间协议校验的最小安全壳 | gw-kiro/converter/websearch | P2流式+资产 | high |
| 路由类 MCP 错误透传判定 | anthropic/websearch.rs:199-207 (should_propagate_mcp_error_text) | 对 quota/account/route_strategy 等错误选择直接透传，而非静默回退 | 有助于把“配置错误”和“临时搜索失败”区分开 | gw-kiro/converter/websearch | P3多进程 | high |
| websearch 输出 token 粗估 | anthropic/websearch.rs:209-211 (estimate_output_tokens) | 用字符数/4 估算摘要输出 token | 若 claude-all-in-one 也要自己合成非模型结果，可作为计费近似 | gw-kiro/converter/websearch | P3多进程 | high |
| websearch 流式事件合成 | anthropic/websearch.rs:213-352 (generate_websearch_events) | 生成 Anthropic 兼容 SSE：先 decision text，再 `server_tool_use` + `input_json_delta`，再 `web_search_tool_result`，最后 summary text 和 usage | 若 claude-all-in-one 计划把外部搜索结果伪装成 Anthropic server tool，这是核心参考 | gw-kiro/converter/websearch | P2流式+资产 | high |
| websearch 非流式 content blocks 合成 | anthropic/websearch.rs:354-380 (create_non_stream_content_blocks) | 非 stream 模式下构造 text + `server_tool_use` + `web_search_tool_result` + summary 的四段内容块 | 便于统一流式/非流式对外形态 | gw-kiro/converter/websearch | P2流式+资产 | high |
| websearch 摘要生成 | anthropic/websearch.rs:383-403 (generate_search_summary) | 将搜索结果列表格式化成文本摘要，包含标题、截断 snippet、来源 URL 和免责声明 | 是 server-side websearch 结果“可读回显层”的现成样板 | gw-kiro/converter/websearch | P2流式+资产 | high |
| websearch result block 构造 | anthropic/websearch.rs:405-429 (create_search_result_blocks) | 把搜索结果转成 `web_search_result` block，snippet 放 `encrypted_content`，并可带 `page_age` | 这是 protected_content 放行逻辑所依赖的数据形状 | gw-kiro/converter/websearch | P2流式+资产 | high |
| 文本截断小工具 | anthropic/websearch.rs:431-435 (truncate_chars) | 按字符边界安全截断 snippet，避免切坏 Unicode | 小工具但很实用，可直接移植 | gw-kiro/converter/websearch | P2流式+资产 | high |
