# static_flow runtime + headers + refresh 簇 — 实现参考

| 能力点 | 参考来源(文件:行号 函数名) | 做什么 | 对kiro-gw价值 | 落到kiro-gw模块 | 建议Phase | confidence |
|---|---|---|---|---|---|---|
| 统一 Kiro 头部注入入口 | `kiro_headers.rs:41 (add_kiro_headers)` | 以 `KiroHeaderConfig` 为中心统一注入鉴权/UA/host/SDK 调用元数据，并按场景附加可选头 | 防止不同请求路径 header 漂移；新项目应避免 generate/MCP/usage/refresh 各自手搓 header | `gw-kiro/headers` | P1 | high |
| x-amz-user-agent 与 user-agent 双轨高保真构造 | `kiro_headers.rs:85 (kiro_user_agents)` | 构造 `x-amz-user-agent=aws-sdk-js/{client_version} KiroIDE-{kiro_ver}-{machine_id}`，以及完整 `user-agent=aws-sdk-js/... ua/2.1 os/... lang/js md/nodejs#... api/{service}#{client_version} KiroIDE-...-{machine_id}` | 这是最核心的发包指纹之一，尤其 machineId 被嵌入 UA；kiro-gw 必须抽成稳定模板，避免简化 | `gw-kiro/headers`, `gw-kiro/machine_id` | P1 | high |
| UA 内 machineId 派生依赖 auth | `kiro_headers.rs:46 (add_kiro_headers)` | 每次发包先从 auth 记录导出 machineId；导不出直接报错，不允许空 machineId UA | 防封关键：UA 不是纯常量，而是账号/设备指纹的一部分 | `gw-kiro/machine_id`, `gw-kiro/headers` | P1 | high |
| Streaming 与 Runtime 服务名区分 | `kiro_headers.rs:14 (KiroAwsService::api_name)` | 将 service 映射为 `codewhispererstreaming` 或 `codewhispererruntime`，影响 UA 中 `api/...` 段 | 新项目需要把 generate/mcp/usage/profile 查询按 service 维度分开，不能全用一个 api tag | `gw-kiro/headers` | P1 | high |
| 头部完整清单与条件开关 | `kiro_headers.rs:50 (add_kiro_headers)` | 条件注入 `content-type`/`accept`/`connection: close`/`x-amzn-kiro-agent-mode`/`x-amzn-codewhisperer-optout`，再统一注入 `x-amz-user-agent`/`user-agent`/`host`/`amz-sdk-invocation-id`/`amz-sdk-request`/`authorization` | 给 kiro-gw 提供一份“默认最像官方”的头部清单；尤其 invocation-id 每次随机、sdk-request 带重试语义 | `gw-kiro/headers` | P1 | high |
| external_idp 特殊 TokenType 头 | `kiro_headers.rs:65 (add_kiro_headers)` | 若 `auth_method()==external_idp`，附加 `TokenType: EXTERNAL_IDP` | 这是 social/IDC 之外的额外身份分流线索，容易漏 | `gw-kiro/headers`, `gw-kiro/token` | P1 | high |
| internal provider 特殊 redirect 头 | `kiro_headers.rs:68 (add_kiro_headers)` | 若 provider 规范化后等于 `internal`，附加 `redirect-for-internal: true` | Internal/Enterprise 账号可能依赖该头命中特定链路；防封与正确路由都重要 | `gw-kiro/headers`, `gw-kiro/token` | P1 | high |
| messages 路径白名单归一 | `provider/kiro_protocol.rs:8 (normalized_kiro_messages_path)` | 仅接受 `/v1/messages`、`/cc/v1/messages` 及其网关前缀别名，并归一为公开路径 | kiro-gw 应把 IR 入站路径与上游 runtime 行为分离，减少分支 | `gw-app/router`, `gw-kiro/protocol` | P1 | high |
| generateAssistantResponse 上游头模板 | `provider/kiro_protocol.rs:15 (add_kiro_upstream_headers)` | Streaming 路径固定 `sdk_request=attempt=1; max=3`、`content-type=application/json`、`accept=application/vnd.amazon.eventstream`、`agent_mode=vibe`、`include_opt_out=true` | generate 路径有一套非常具体的头模板；agent_mode 与 optout 都是高价值指纹 | `gw-kiro/headers`, `gw-kiro/dispatch` | P1 | high |
| MCP 上游头模板 | `provider/kiro_protocol.rs:36 (add_kiro_mcp_headers)` | MCP 也走 Streaming service，但 `accept=None`、`agent_mode=None`、`include_opt_out=false`，并按需注入 `x-amzn-kiro-profile-arn` | 说明 MCP 与主对话不是同一 header 模板，不能偷懒复用 generate 头 | `gw-kiro/headers`, `gw-kiro/dispatch` | P1 | high |
| profileArn 在 MCP 头中单独承载 | `provider/kiro_protocol.rs:57 (add_kiro_mcp_headers)` | 若 profileArn 非空，则补 `x-amzn-kiro-profile-arn` 请求头 | 这是 profileArn 双通道之一；MCP 走 header，主 generate 走 body 注入 | `gw-kiro/headers`, `gw-kiro/token` | P1 | high |
| auth 上下文获取与 refresh 统一入口 | `kiro_refresh.rs:73 (ensure_context_for_route)` | 对 runtime/generate 获取 `KiroCallContext{auth,access_token}`，必要时刷新、补 profileArn 并落库 | 建议 kiro-gw 也做 `ensure_call_context` 层，避免 dispatch 层自己关心 refresh 细节 | `gw-kiro/token` | P1 | high |
| MCP/usage 强制要求 profileArn | `kiro_refresh.rs:87 (ensure_context_for_route_requiring_profile)` | 在需要 profileArn 的路径上，如果 auth 中缺失则强制解析 | 把“需要 profileArn”变成显式契约，而不是散落 bool 判断 | `gw-kiro/token`, `gw-kiro/dispatch` | P1 | high |
| refresh 串行锁，按账号去重 | `kiro_refresh.rs:119 (ensure_context_for_route_with_profile_requirement)`, `kiro_refresh.rs:796 (refresh_lock_for_account)` | 每个 account 一个 async mutex，避免并发请求同时刷新同一账号 | 防止 refresh 风暴、token 覆盖竞争、profileArn 竞态 | `gw-kiro/token`, `gw-store/sqlite` | P1 | high |
| refresh 前先判断 access token 是否快过期 | `kiro_refresh.rs:589 (needs_refresh)` | 通过 `expires_at` 或 JWT `exp` 解析，提前 10 分钟触发刷新 | 这是稳态行为的关键，能减少运行时 401/403 | `gw-kiro/token` | P1 | high |
| access token 缺失直接视为需刷新 | `kiro_refresh.rs:589 (needs_refresh)`, `kiro_refresh.rs:580 (non_empty_access_token)` | token 空/不存在时直接进入 refresh 流程；仍取不到则错误 | 简化状态机，避免发空 Bearer 请求污染指纹 | `gw-kiro/token` | P1 | high |
| social vs IDC 刷新分流 | `kiro_refresh.rs:607 (refresh_auth)` | `idc/builder-id/iam` 走 IDC 刷新，其他方法走 social 刷新 | 这是用户特别关心的 social/IdC 分流主判定 | `gw-kiro/token` | P1 | high |
| refresh token 完整性校验 | `kiro_refresh.rs:620 (validate_refresh_token)` | 要求 refresh token 非空、长度足够、不能像 `...` 截断串 | 新项目应在 refresh 前就拦掉明显坏数据，避免上游探测性失败 | `gw-kiro/token`, `gw-store/sqlite` | P1 | high |
| social refresh 请求指纹 | `kiro_refresh.rs:635 (refresh_social)`, `kiro_refresh.rs:806 (social_refresh_user_agent)` | 请求 `https://prod.{region}.auth.desktop.kiro.dev/refreshToken`，带 `accept: application/json, text/plain, */*`、`content-type: application/json`、`user-agent: KiroIDE-{ver}-{machine_id}`、`accept-encoding: gzip, compress, deflate, br`、`host`、`connection: close` | social refresh 用的是另一套非常“桌面端”的 UA，不是 aws-sdk-js 模板，必须保真 | `gw-kiro/token`, `gw-kiro/headers` | P1 | high |
| IDC refresh 请求指纹 | `kiro_refresh.rs:688 (refresh_idc)`, `kiro_refresh.rs:810 (idc_refresh_amz_user_agent)`, `kiro_refresh.rs:814 (idc_refresh_user_agent)` | 请求 `https://oidc.{region}.amazonaws.com/token`，带 `content-type`、`host`、`amz-sdk-invocation-id`、`amz-sdk-request=attempt=1; max=4`、`connection: close`、`x-amz-user-agent=aws-sdk-js/3.980.0 KiroIDE-{ver}`、`accept:*/*`、`user-agent=aws-sdk-js/... ua/2.1 os/... lang/js md/nodejs#... m/E` | IDC refresh 走纯 AWS/OIDC 风格指纹，与 social 明显不同；这是强参考 | `gw-kiro/token`, `gw-kiro/headers` | P1 | high |
| invalid_grant 视为永久失效并禁用账号 | `kiro_refresh.rs:135 (ensure_context_for_route_with_profile_requirement)`, `kiro_refresh.rs:790 (is_invalid_refresh_token_grant)` | 若刷新接口返回 `400 + invalid_grant + Invalid refresh token provided`，则写回 disabled/disabled_reason，并保存 last_error | kiro-gw 需要把“可重试刷新失败”和“永久失效”分开，否则会反复打坏 token | `gw-store/sqlite`, `gw-kiro/token`, `gw-app/admin` | P1 | high |
| 成功刷新后合并持久化 auth JSON | `kiro_refresh.rs:410 (persist_active_kiro_auth_update)`, `kiro_refresh.rs:543 (refreshed_auth_json)` | 刷新成功后保留原 auth_json 其他字段，只 merge 更新 access/refresh/profile/状态等 | 对新项目很有价值：存储层别覆盖掉额外元数据 | `gw-store/sqlite`, `gw-kiro/token` | P1 | high |
| provider 规范化 | `kiro_refresh.rs:249 (normalized_token_provider)` | 统一 `github/google/builder-id/aws/enterprise/internal/external_idp` 等 provider 名称 | 有助于 profileArn 规则、header 分流、refresh 策略保持一致 | `gw-kiro/token`, `gw-core` | P1 | high |
| supports_profiles 判定 | `kiro_refresh.rs:231 (supports_profiles)` | `Enterprise/Internal/BuilderId/ExternalIdp/social` 都认为支持 profileArn 逻辑 | 帮助 kiro-gw 明确哪些账号需要 profileArn 解析链 | `gw-kiro/token` | P1 | high |
| 固定 profileArn 快速路径 | `kiro_refresh.rs:241 (fixed_profile_arn)` | `BuilderId -> arn:...AAAACCCCXXXX`，`Github/Google -> arn:...EHGA3GRVQMUK` | 某些 provider 无需远端查询；这能减少请求并更像官方行为 | `gw-kiro/token` | P1 | high |
| 动态 profileArn 回源查询 | `kiro_refresh.rs:188 (resolve_profile_arn_for_auth)`, `kiro_refresh.rs:278 (fetch_profile_arn_from_backend)` | 当前 auth 无 profileArn 时，必要情况下调用 backend 拉取 profile 候选，成功后回写 auth | 这是 profileArn 处理的完整补全链路，很适合新项目照抄为 token 子模块 | `gw-kiro/token` | P1 | high |
| external_idp 的 profile 区域枚举 | `kiro_refresh.rs:358 (profile_regions_to_query)` | external_idp 会遍历 `KIRO_STANDARD_PROFILE_REGIONS` 全集去查可用 profiles；其他账号按 auth region 所属 partition 过滤 | Internal/ExternalIdP 账号 profileArn 查找范围更大，是值得保留的经验 | `gw-kiro/token` | P2 | high |
| profile 列表查询的请求模板 | `kiro_refresh.rs:299 (fetch_profile_candidates_for_region)` | 调 `.../ListAvailableProfiles`，使用 `add_kiro_headers` 的 Runtime service，`client_version=1.0.0`、`sdk_request=attempt=1; max=1`、`content-type/accept=application/json`、`connection_close=true` | profile 查询不是裸 reqwest GET/POST，而是沿用 runtime 头模板；kiro-gw 可以复用 headers 子模块 | `gw-kiro/token`, `gw-kiro/headers` | P2 | high |
| runtime/management 上游基址分离 | `kiro_refresh.rs:504 (runtime_upstream_base_url)`, `kiro_refresh.rs:511 (management_upstream_base_url)` | runtime 默认 `https://runtime.{region}.kiro.dev`，management 默认 `https://management.{region}.kiro.dev`，且支持环境变量覆盖 | 新项目应将 generate/mcp 与 usage/status/profile 路由拆开建模，而不是一个 base URL 走到底 | `gw-core`, `gw-app/config`, `gw-kiro/dispatch` | P1 | high |
| split env + legacy env 兼容 | `kiro_refresh.rs:530 (configured_upstream_base_url)`, `kiro_refresh.rs:536 (read_upstream_env)` | 先读 family-specific env，再回退 legacy `KIRO_UPSTREAM_BASE_URL`，且自动 trim 尾斜杠 | 适合做迁移期配置兼容；不会影响发包路径拼接与 host 头 | `gw-app/config`, `gw-kiro/dispatch` | P2 | high |
| host 头从 URL 解析而来 | `kiro_refresh.rs:518 (upstream_host_header)` | 从上游 URL 提取 host[:port] 作为显式 `host` header | 新项目应避免手写 host 常量，特别是 split env/本地测试场景 | `gw-kiro/headers` | P1 | high |
| usage 查询也走同一代理出口 | `kiro_refresh.rs:454 (fetch_usage_limits_for_route)` | `fetch_usage_limits_for_route` 内部通过 `provider_client(route.proxy.as_ref())` 建 client，再请求 management API | 说明 usage/status 与主发包要共享同一 proxy 语义，避免出口指纹不一致 | `gw-app/egress`, `gw-kiro/token`, `gw-kiro/dispatch` | P1 | high |
| usage 查询带 profileArn query 参数 | `kiro_refresh.rs:454 (fetch_usage_limits_for_route)` | `/getUsageLimits?origin=AI_EDITOR&resourceType=AGENTIC_REQUEST`，若有 profileArn 则 URL encode 后拼进 query | profileArn 在 usage 路径走 query，不是 header/body；新项目需按 endpoint 区分注入位置 | `gw-kiro/token`, `gw-kiro/dispatch` | P1 | high |
| usage 查询的头模板 | `kiro_refresh.rs:477 (fetch_usage_limits_for_route)` | 使用 Runtime service `add_kiro_headers`，`client_version=1.0.0`、`sdk_request=attempt=1; max=1`、`connection_close=true` | 这能让状态探测包和 generate 包维持同一族指纹，而不是裸 GET | `gw-kiro/headers`, `gw-kiro/token` | P1 | high |
| runtime 发包前 profileArn 注入到 body | `provider/kiro_dispatch.rs:1430 (send_kiro_generate_request)`, `provider/kiro_dispatch.rs:1467 (kiro_request_body_with_profile_arn)` | generate 前把 profileArn 注入 JSON body 字段 `profileArn`；若空则删掉该字段 | 主消息路径 profileArn 走 body，非常重要；与 MCP/usage 方式不同 | `gw-kiro/dispatch`, `gw-kiro/token` | P1 | high |
| generate/mcp 发包前都通过 ensure_context 拉 auth | `provider/kiro_dispatch.rs:1214 (call_kiro_generate_for_route)`, `provider/kiro_dispatch.rs:1306 (call_kiro_mcp_for_route)` | 每次上游调用前先拿 call context；401/403 时可 force_refresh 再来一轮 | 说明 refresh 与 dispatch 耦合点应在“发包前最后一层”，而不是更外层预刷新 | `gw-kiro/dispatch`, `gw-kiro/token` | P1 | high |
| generate 与 MCP 都有 3 次本地重试预算 | `provider/kiro_dispatch.rs:1221 (call_kiro_generate_for_route)`, `provider/kiro_dispatch.rs:1316 (call_kiro_mcp_for_route)` | transport/server/timeout/429 等某些失败会在同路由内 sleep 350ms 重试，最多 3 次 | 防封和稳定性层面值得参考；避免过早切号 | `gw-kiro/dispatch`, `gw-kiro/scheduler` | P2 | high |
| 401/403 触发 force_refresh 一次 | `provider/kiro_dispatch.rs:1279 (call_kiro_generate_for_route)`, `provider/kiro_dispatch.rs:1382 (call_kiro_mcp_for_route)` | 首次 401/403 不立刻切号，而是强制 refresh 后重试同号；若仍失败再走 RetryNext | 这是账号状态机的重要细节，能减少因 token 过期导致的无谓切号 | `gw-kiro/dispatch`, `gw-kiro/token`, `gw-kiro/scheduler` | P1 | high |
| 状态码 -> QuotaExhausted 映射 | `provider/kiro_dispatch.rs:1258 (call_kiro_generate_for_route)`, `provider/kiro_dispatch.rs:1362 (call_kiro_mcp_for_route)` | `402 + monthly request limit` 判为 `QuotaExhausted` | 切号策略与持久化状态依赖该分类；建议在 gw-core 定义统一枚举 | `gw-core`, `gw-kiro/dispatch`, `gw-store/sqlite` | P1 | high |
| 状态码 -> RateLimited(account) 映射 | `provider/kiro_dispatch.rs:1262 (call_kiro_generate_for_route)`, `provider/kiro_dispatch.rs:1366 (call_kiro_mcp_for_route)` | `429 + daily_request_limit_cooldown(body)` 判为 `RateLimited{cooldown, mark_proxy:false}` | 表示 429 不只是“失败”，还会给冷却时间并保留代理状态 | `gw-core`, `gw-kiro/dispatch`, `gw-kiro/scheduler` | P1 | high |
| 400 transient invalid model -> proxy 级冷却 | `provider/kiro_dispatch.rs:1270 (call_kiro_generate_for_route)`, `provider/kiro_dispatch.rs:1374 (call_kiro_mcp_for_route)` | 若 400 body 命中 transient invalid model，则记作 `RateLimited{cooldown, mark_proxy:true}` | 这是很有价值的经验：某些 400 不是 payload 错，而是出口/代理问题，需要 proxy 冷却 | `gw-kiro/dispatch`, `gw-kiro/scheduler`, `gw-app/egress` | P1 | high |
| RetryNext / Fatal 分类 | `provider/kiro_error.rs:8 (KiroRouteFailureKind)`, `provider/kiro_dispatch.rs:1493 (should_failover_after_kiro_route_failure)` | 把失败抽象成 `RetryNext/Fatal/QuotaExhausted/RateLimited`，供切号/冷却器统一消费 | 这是新项目值得直接抽象进 gw-core 的错误契约 | `gw-core`, `gw-kiro/dispatch`, `gw-kiro/scheduler` | P1 | high |
| QuotaExhausted 按 routing_identity 整簇封禁 | `provider/kiro_dispatch.rs:1501 (should_failover_after_kiro_route_failure)`, `provider/kiro_dispatch.rs:1537 (account_names_for_kiro_routing_identity)` | 命中 quota 后，把同一 `routing_identity` 下的所有 account 都记为 exhausted | “refresh簇”最值得借鉴的能力点之一：同身份多号共享配额/状态，不是只封单号 | `gw-store/sqlite`, `gw-kiro/scheduler`, `gw-app/admin` | P1 | high |
| RateLimited 冷却同时可选 proxy 冷却 | `provider/kiro_dispatch.rs:1514 (should_failover_after_kiro_route_failure)` | 标记 account cooldown；若 `mark_proxy=true` 且 route 有 proxy，则同步标记 proxy cooldown | 非常适合 kiro-gw 的 egress 设计：账号和出口要双维度状态机 | `gw-kiro/scheduler`, `gw-app/egress`, `gw-store/sqlite` | P1 | high |
| RetryNext 只踢出当前账号 | `provider/kiro_dispatch.rs:1531 (should_failover_after_kiro_route_failure)` | 普通可重试失败只把当前 account 加入 failed_accounts，尝试剩余候选 | 建议新项目保留这一层级，避免扩大故障域 | `gw-kiro/scheduler` | P2 | high |
| 候选账号存在性判定 | `provider/kiro_dispatch.rs:1482 (has_remaining_kiro_candidate)` | 用于判断当前失败后是否还值得继续 failover | 简单但实用，避免错误地回环重试 | `gw-kiro/scheduler` | P2 | high |
| 空流首帧探测与延迟首事件 | `provider/kiro_dispatch.rs:1080 (peek_kiro_stream)`, `provider/kiro_dispatch.rs:1117 (prepare_kiro_stream_response_for_route)` | 先缓冲到首个完整 eventstream frame 再向下游出字节；空流/半帧/解码失败先在服务端处理 | 对 kiro-gw 的 `stream/parser` 很重要：能在不污染下游会话的情况下做重试/切号 | `gw-kiro/stream`, `gw-kiro/parser`, `gw-kiro/dispatch` | P1 | high |
| 空流透明重试 | `provider/kiro_dispatch.rs:1127 (prepare_kiro_stream_response_for_route)` | 对空流最多 `KIRO_EMPTY_STREAM_MAX_RETRIES` 次，退避 `200ms * attempt`，成功后继续同号返回 | 这是已验证有效的上游抖动修复策略，强烈值得参考 | `gw-kiro/stream`, `gw-kiro/dispatch` | P1 | high |
| content_length_exceeded 提前转 413 | `provider/kiro_dispatch.rs:1130 (prepare_kiro_stream_response_for_route)` | 若首段 chunk 已显示 content length exceeded，则直接合成 prompt-too-long 响应 | 可提前止损，避免把上游怪错原样泄漏到客户端 | `gw-kiro/stream`, `gw-kiro/error` | P2 | high |
| route 选择前先拿 key/account permit | `provider/kiro_dispatch.rs:400 (dispatch_kiro_proxy)`, `provider/kiro_dispatch.rs:422 (dispatch_kiro_proxy)` | 先做 key 级并发/起始间隔限制，再做 account permit 与 route 选择 | 对 gw-app worker 多进程与调度器设计有直接参考价值 | `gw-app/worker`, `gw-kiro/scheduler` | P2 | medium |
| 会话亲和优先账号选择 | `provider/kiro_dispatch.rs:410 (dispatch_kiro_proxy)`, `provider/kiro_dispatch.rs:769 (dispatch_kiro_proxy)` | 根据 session -> account affinity 查 preferred account；成功后记忆亲和 | 防封与真实 cache hit 都依赖“同会话同账号” | `gw-kiro/scheduler`, `gw-store/sqlite` | P1 | high |
| 新会话按绑定数均衡 | `provider/kiro_dispatch.rs:418 (dispatch_kiro_proxy)` | 若有 session id 但无既有 affinity，且多账号可用，则按各账号已绑定 session 数均衡分配 | 兼顾 cache 命中和负载均衡，是不错的默认策略 | `gw-kiro/scheduler` | P2 | high |
| request_input_tokens 本地估算 | `provider/kiro_dispatch.rs:227 (dispatch_kiro_proxy)` | 对 system/messages/tools 统一算 token，用于后续 compaction/usage 估算 | 新项目的 cache_sim / compact gate 都需要这类前置计数 | `gw-kiro/cache_sim`, `gw-kiro/converter` | P2 | medium |
| 远程媒体 URL 预解析与禁用时剥离 | `provider/kiro_dispatch.rs:188 (dispatch_kiro_proxy)`, `provider/kiro_media.rs:103 (resolve_kiro_remote_media_sources)`, `provider/kiro_media.rs:123 (strip_kiro_remote_media_sources)` | key 开启时把 image/document 的 URL source 拉取并转成本地 base64/text；未开启时直接剥离并告警 | 不直接属于 header，但会影响上游实际 payload 指纹与 400 风险，值得纳入 converter/media 子模块 | `gw-kiro/converter`, `gw-kiro/media` | P3 | high |
| remote media SSRF 防护 | `provider/kiro_media.rs:448 (validate_kiro_remote_media_url)`, `provider/kiro_media.rs:481 (validate_kiro_remote_media_resolved_addresses)`, `provider/kiro_media.rs:583 (PrivateFilteringDnsResolver)` | 限定 http/https，拒绝 localhost/private IP，并用自定义 DNS resolver 防 rebinding | 如果 kiro-gw 以后支持 URL 媒体，这套防护很值得直接迁移 | `gw-kiro/media` | later | high |
| Kiro 错误类型与用户可见消息脱敏 | `provider/kiro_error.rs:74 (kiro_error_type_for_status)`, `provider/kiro_error.rs:85 (kiro_default_user_error_message)`, `provider/kiro_error.rs:101 (kiro_user_visible_message)` | 将状态码映射到 anthropic 风格 error type，并对 429/402/5xx 等隐藏上游细节 | 对新网关有价值：对外保持 Anthropic IR，一致化错误面 | `gw-kiro/error`, `gw-core` | P2 | high |
| Bedrock/ValidationException 风格错误桥接 | `provider/kiro_error.rs:138 (kiro_bedrock_exception_message)`, `provider/kiro_error.rs:147 (kiro_bedrock_anthropic_error_body)` | 把内部/上游异常包成 anthropic json error，同时保留 Bedrock exception flavor | 对 protected content、validation 之类桥接场景很有用 | `gw-kiro/error` | P3 | medium |
| 非成功上游响应统一包装 | `provider/kiro_error.rs:168 (kiro_upstream_error_response)` | 从上游 bytes 总结错误文本，再转成统一 anthropic 错误响应 | 减少调用点重复逻辑 | `gw-kiro/error`, `gw-kiro/dispatch` | P2 | high |
| usage 记录摘要构建 | `provider/kiro_usage.rs:27 (build_kiro_usage_summary)` | 根据请求 token、context token、credit_usage、cache 策略/模拟器构建 uncached/cached/output 摘要 | 对你新项目的 cache_sim 与 billing 很有参考价值，尤其运行时如何落 usage | `gw-kiro/cache_sim`, `gw-kiro/usage`, `gw-store/sqlite` | P3 | medium |
| 兼容 Anthropic usage 字段的回填 | `provider/kiro_usage.rs:72 (anthropic_usage_json_with_policy)` | 把 Kiro 侧 cached/uncached 估算回填成 `input_tokens/cache_creation_input_tokens/cache_read_input_tokens` | 因为内部 IR 是 Anthropic Messages，这个桥接很契合 kiro-gw 目标 | `gw-kiro/usage`, `gw-core` | P3 | high |
| 零缓存/错误请求时捕获 full request | `provider/kiro_usage.rs:218 (record_kiro_usage)` | 状态>=400、full_request_logging、zero_cache_debug 等条件下记录 request headers/body/upstream body/error/timing | 对防封排障很有价值：建议新项目在 admin/debug 模式保留类似审计抓手 | `gw-store/sqlite`, `gw-app/admin` | P3 | high |
| websearch MCP usage 单独记账 | `provider/kiro_usage.rs:280 (record_kiro_websearch_usage)` | MCP websearch 不走 credit_usage，而走模型倍率 billable_tokens | 新项目若保留 MCP 功能，可将 websearch 看作独立 usage 支路 | `gw-kiro/usage`, `gw-kiro/dispatch` | P4 | medium |
| provider client 构造支持 proxy cache | `provider/client.rs:14 (build_provider_client)`, `provider/client.rs:31 (provider_client)` | 无 proxy 走默认 client；有 proxy 则按 `proxy_url+用户名+密码` 做 client cache，且设置 idle timeout/max idle/keepalive | 这是主发包 client 的标准做法，能保持出口复用与连接池稳定 | `gw-app/egress`, `gw-kiro/dispatch` | P1 | high |
| provider client 只支持 proxy，不支持 local_address 绑定 | `provider/client.rs:14 (build_provider_client)` | builder 仅设置 pool/keepalive/proxy，没有 `local_address` 或 `interface` 绑定 | 这是关键结论：static_flow 运行时发包链靠 proxy 一致性，不靠本机源地址绑定 | `gw-app/egress` | P1 | high |
| refresh 内部 client 与主 provider client 不同 | `kiro_refresh.rs:749 (provider_client)` | refresh 模块自带一个轻量 `reqwest::Client::builder()+proxy`，没有池参数，也没有复用 `provider/client.rs` 的缓存 client | 说明 static_flow 在 refresh 上只保证“同 proxy”，不保证同连接池；kiro-gw 可考虑统一，但要知道参考实现并未完全共用 client | `gw-app/egress`, `gw-kiro/token` | P2 | high |
| refresh/usage/generate/MCP 全部从 route.proxy 取出口 | `provider/kiro_dispatch.rs:1435 (send_kiro_generate_request)`, `provider/kiro_dispatch.rs:1454 (send_kiro_mcp_request)`, `kiro_refresh.rs:282 (fetch_profile_arn_from_backend)`, `kiro_refresh.rs:475 (fetch_usage_limits_for_route)`, `kiro_refresh.rs:645 (refresh_social)`, `kiro_refresh.rs:705 (refresh_idc)` | 各条链路都调用 `provider_client(route.proxy.as_ref())` 或 refresh 内同等逻辑 | 这是“代理一致性(refresh/usage/发包同出口)”的直接证据，值得 kiro-gw 明确保留 | `gw-app/egress`, `gw-kiro/token`, `gw-kiro/dispatch` | P1 | high |
| latency routing 打分基于 account+proxy 混合 | `kiro_latency.rs:14 (ACCOUNT_WEIGHT/PROXY_WEIGHT)`, `kiro_latency.rs:104 (PreparedKiroLatencySnapshot::route_score)` | 路由分数 = 0.7*account 延迟 + 0.3*proxy 延迟，缺样本回退 global avg | 对“账号状态”和“出口状态”双维调度很有参考价值 | `gw-kiro/scheduler`, `gw-app/egress` | P3 | high |
| latency band 而非绝对值选路 | `kiro_latency.rs:67 (KiroLatencyRanker::route_score_band)` | 将延迟量化到 band，band 0 是 `global_avg ± 15%` 容忍区间，近似持平的账号不因小差异被饿死 | 适合多账号防封：减少某个“最快号”被打爆 | `gw-kiro/scheduler` | P3 | high |
| latency 快照过期保护 | `kiro_latency.rs:88 (KiroLatencyRanker::current_snapshot)` | 快照超过 5 分钟视为 stale，不参与路由 | 避免旧数据误导调度 | `gw-kiro/scheduler` | P3 | high |
| latency 平滑 | `kiro_latency.rs:185 (smoothed_latency_ms)` | 用全局均值 + `SMOOTHING_SAMPLES=5` 做贝叶斯式平滑 | 样本少的账号/代理不会被偶然值拉偏 | `gw-kiro/scheduler` | P3 | high |
| latency 快照后台刷新 | `kiro_latency.rs:191 (spawn_kiro_latency_refresher)`, `kiro_latency.rs:209 (refresh_once)` | 每 60 秒从 `/internal/kiro-gateway/latency-ranking?source=hot&window=1h` 拉一份排序快照 | kiro-gw 若做 admin/worker 多进程，可把延迟画像做成独立后台任务 | `gw-app/worker`, `gw-app/admin`, `gw-kiro/scheduler` | P4 | medium |
| 账号状态定时刷新循环 | `kiro_status.rs:17 (spawn_kiro_status_refresher)`, `kiro_status.rs:52 (refresh_all_kiro_statuses)` | 启动初次 warmup + 周期 refresh，按随机 delay 与 account jitter 轮询账号 usage/status | 新项目可把状态刷新做成后台 worker，而不是请求时临时探测 | `gw-app/worker`, `gw-store/sqlite`, `gw-app/admin` | P2 | high |
| status cache 状态机 | `kiro_status.rs:132 (build_ready_status_update)`, `kiro_status.rs:155 (build_error_status_update)`, `kiro_status.rs:181 (build_disabled_status_update)` | 维护 `ready/degraded/error/disabled` 状态、last_checked/last_success/error_message/expires_at | admin 面板和调度器都可消费；能区分“有旧余额但本次失败”的 degraded | `gw-store/sqlite`, `gw-app/admin`, `gw-kiro/scheduler` | P2 | high |
| 余额视图转换 | `kiro_status.rs:203 (admin_kiro_balance_from_usage)` | 从 usage breakdown 汇总 `current_usage/usage_limit/remaining/next_reset/subscription_title/user_id` | 适合新项目后台面板和调度策略使用 | `gw-store/sqlite`, `gw-app/admin` | P2 | high |
| 刷新间隔与账号抖动随机化 | `kiro_status.rs:220 (next_kiro_refresh_delay)`, `kiro_status.rs:235 (next_kiro_account_jitter)` | 周期间隔在 min/max 随机，账号间也插入 0..jitter 的随机 sleep | 有助于减少整点整批刷新形成固定探针流量 | `gw-app/worker` | P2 | high |
| request context 中间件打 request_id/trace_id | `request_context.rs:14 (request_context_middleware)` | 读取或生成 request_id/trace_id，写回响应头并记 access log | 对 kiro-gw 的 admin/debug 很实用，但与上游防封关联较弱 | `gw-app/router`, `gw-app/admin` | P4 | medium |
| access log 含 remote_addr/path/status/elapsed | `request_context.rs:52 (request_context_middleware)` | 输出一条统一 access log，包含耗时和远端地址 | 有利于关联某次上游冷却/失败与客户端来源 | `gw-app/router`, `gw-app/admin` | P4 | medium |
| 当前参考实现未做 local source address 绑定 | `provider/client.rs:17 (build_provider_client)`, `kiro_refresh.rs:750 (provider_client)` | 无论主发包还是 refresh 内部 client，都没看到 `local_address` 绑定逻辑 | 如果 kiro-gw 计划做多出口绑定，这将是新增能力，不是 static_flow 现成做法 | `gw-app/egress` | P1 | high |
| 主发包与 refresh 的 SDK version 常量分离 | `provider/kiro_protocol.rs:5 (KIRO_PROVIDER_AWS_SDK_VERSION)`, `kiro_refresh.rs:28 (KIRO_USAGE_AWS_SDK_VERSION)`, `kiro_refresh.rs:29 (KIRO_PROFILES_AWS_SDK_VERSION)`, `kiro_refresh.rs:30 (KIRO_IDC_AWS_SDK_VERSION)` | generate/MCP、usage/profile、IDC refresh 使用不同 client version 常量 | 说明不要强行用一个版本号覆盖所有链路，版本漂移本身也是指纹 | `gw-kiro/headers`, `gw-core` | P1 | high |
| machineId 只在 social refresh 和 runtime 头中显式出现 | `kiro_headers.rs:46 (add_kiro_headers)`, `kiro_refresh.rs:646 (refresh_social)`, `kiro_refresh.rs:806 (social_refresh_user_agent)` | runtime/generate/MCP/usage/profile 查询都把 machineId 嵌在 UA；IDC refresh 不带 machineId，social refresh 带 | 这个差异很容易忽略，但对真实客户端拟态很重要 | `gw-kiro/machine_id`, `gw-kiro/headers`, `gw-kiro/token` | P1 | high |
