# xkiro.rs 可借鉴模块 — 实现参考

| 能力点 | 参考来源(文件:行号 函数名) | 做什么 | 对claude-all-in-one价值 | 落到claude-all-in-one模块 | 建议Phase | confidence |
|---|---|---|---|---|---|---|
| 工具定义超阈值双阶段压缩 | `anthropic/tool_compression.rs:17 (compress_tools_if_needed)` | 当 tools 总序列化体积超过 20KB 时，先递归瘦身 `input_schema`，再按比例截断 `description`，同时保留 enum / nested properties / items 等结构骨架。 | 可直接缓解大 toolset 请求体膨胀，尤其适合 Claude Code / MCP 大户，降低上游 400 与 cache miss 风险。 | `gw-kiro::converter` 或 `gw-kiro::tooling::compression` | P1 | high |
| JSON Schema 结构骨架保留策略 | `anthropic/tool_compression.rs:96 (simplify_schema)`; `anthropic/tool_compression.rs:110 (simplify_json_schema)` | 保留 `$schema`/`type`/`required`/`additionalProperties`，对子属性只保留必要的类型、枚举、数组 items 与嵌套结构。 | 比粗暴删 schema 更稳，能在瘦身同时维持工具可调用性，适合迁移到 Anthropic Messages IR -> Kiro 请求转换层。 | `gw-kiro::converter` | P1 | high |
| ConversationState 压缩总管道 | `anthropic/compressor.rs:38 (compress)` | 对发送前 `ConversationState` 做分层压缩：空白压缩、thinking 处理、tool_result 截断、tool_use input 截断、history 截断，最后统一修复配对与空内容。 | 这是非常完整的发送前瘦身总线，能直接作为 `gw-kiro` 的 payload hardening 参考蓝本。 | `gw-kiro::compressor` | P1 | high |
| 空白压缩且跳过占位符 | `anthropic/compressor.rs:100 (compress_whitespace)`; `anthropic/compressor.rs:148 (compress_string_field)` | 合并多余空行、去尾空格，但跳过仅为 API 占位的单空格 content。 | 能安全减小上下文体积，不破坏 Kiro 对非空 content 的特殊要求。 | `gw-kiro::compressor` | P2 | high |
| thinking 历史块丢弃/截断 | `anthropic/compressor.rs:166 (compress_thinking_pass)`; `anthropic/compressor.rs:190 (remove_thinking_blocks)`; `anthropic/compressor.rs:207 (truncate_thinking_blocks)` | 对历史 assistant 的 `<thinking>...</thinking>` 做 discard 或 truncate 策略。 | 对长对话压缩 ROI 很高，且对最终回答语义影响通常较小。 | `gw-kiro::compressor` | P2 | high |
| tool_result 智能头尾截断 | `anthropic/compressor.rs:241 (smart_truncate_by_lines)`; `anthropic/compressor.rs:293 (compress_tool_results_pass)` | 按字符阈值保留头尾若干行，并插入 omitted 标记，避免整段工具输出拖爆请求体。 | 适合 Read/Bash/Web fetch 等长输出回传；比纯 head 截断更利于模型继续工作。 | `gw-kiro::compressor` | P1 | high |
| tool_use input 深度字符串截断 | `anthropic/compressor.rs:356 (compress_tool_use_inputs_pass)`; `anthropic/compressor.rs:376 (truncate_json_value_strings)` | 遍历 tool_use JSON input，递归截断过长字符串字段，并避免“截断标记导致反而变长”的边界问题。 | 对 Write/Edit/structured tool call 的超长 JSON 很实用，可减少上游解析失败与 body 超限。 | `gw-kiro::compressor` | P2 | high |
| 历史轮次裁剪并保留 system pair | `anthropic/compressor.rs:425 (compress_history_pass)` | 保留前两条 system pair，从前往后按 user+assistant 成对裁历史，支持按 turn 数或字符数裁剪。 | 很适合在 Kiro 会话状态里做最后兜底，避免把首轮 system 注入打散。 | `gw-kiro::compressor` | P2 | high |
| tool_use / tool_result 配对修复 | `anthropic/compressor.rs:488 (repair_tool_pairing_pass)` | 压缩后重扫历史与 current，删掉孤立 `tool_result` 与孤立 `tool_use`，保证 Kiro 严格配对要求。 | 这是 Kiro 兼容性的关键安全阀，强烈建议保留。 | `gw-kiro::parser` 或 `gw-kiro::compressor` | P1 | high |
| 空 content 最终修复兜底 | `anthropic/compressor.rs:571 (repair_non_empty_content_pass)`; `anthropic/compressor.rs:637 (repair_tool_result_text_fields)`; `anthropic/compressor.rs:669 (repair_content_field)` | 只在必要时把空文本修成 `.`，同时清掉 tool_result 中空 text 项。 | 能防止“压缩后空内容”触发上游确定性 400，是生产实战型细节。 | `gw-kiro::compressor` | P1 | high |
| 超长用户消息末级截断 | `anthropic/compressor.rs:685 (compress_long_messages_pass)`; `anthropic/compressor.rs:711 (truncate_long_content)` | 对 user content 做最后手段截断，保留头部并追加 truncation notice。 | 适合作为自适应二次压缩 fallback，而不是默认常开。 | `gw-kiro::compressor` | P3 | medium |
| Grapheme 级安全截断 | `anthropic/compressor.rs:733 (safe_char_truncate)` | 用 `unicode_segmentation` 按 grapheme cluster 截断，避免 emoji / combining char 被切坏。 | 这是所有文本截断都值得复用的低层工具。 | `gw-core::text` 或 `gw-kiro::text` | P1 | high |
| 截断恢复 system notice 注入 | `anthropic/truncation.rs:9 (TRUNCATION_RECOVERY_SYSTEM_NOTICE)` | 给模型解释 `[System Notice]` / `[API Limitation]` 是合法系统通知，不是 prompt injection。 | 若你计划做“软失败 + 让模型重试”的闭环，这个 notice 很有价值。 | `gw-kiro::truncation` | P3 | medium |
| tool call 截断启发式检测 | `anthropic/truncation.rs:61 (detect_truncation)`; `anthropic/truncation.rs:74 (detect_truncation_with_required)` | 根据空输入、未闭合字符串、括号不平衡、缺少必填字段等启发式判定 tool JSON 被截断。 | 对 Kiro / Anthropic 流式工具调用都通用，能把“坏 JSON”从硬错误转成可恢复错误。 | `gw-kiro::parser::truncation` | P1 | high |
| 软失败文案生成 | `anthropic/truncation.rs:138 (build_soft_failure_result)` | 为不同截断类型生成提示模型分块重试的错误消息。 | 可与 tool_result error path 结合，改善大文件写入/大 JSON 调用体验。 | `gw-kiro::parser::truncation` | P3 | medium |
| 内置关键工具必填字段映射 | `anthropic/truncation.rs:177 (required_fields_for)` | 对 `Write` / `Edit` / `Read` / `Bash` 等内置工具规定 required 字段，用于 MissingFields 检测。 | 若 claude-all-in-one 支持 Anthropic 风格内置工具，这张表很值得直接移植并扩展。 | `gw-core::tool_contract` | P2 | high |
| system prompt Layer-1 清洗入口 | `anthropic/prompt_filter.rs:37 (apply_prompt_filters)` | 对 system prompt 先做 Claude Code 检测替换、边界线剥离、环境噪音清洗、限制段剥离，再跑用户自定义规则。 | 非常贴近你现在反代场景，能减少客户端噪音、环境段和限制注入对 conversationId / cache 的扰动。 | `gw-kiro::prompt_filter` | P1 | high |
| Claude Code 提示识别与缩写替换 | `anthropic/prompt_filter.rs:70 (is_claude_code_system_prompt)` | 用 marker 命中数判定 Claude Code 系统提示，满足阈值时整体替换为精简 backend prompt。 | 对抗大段 CLI 自带 prompt 噪音很有效，也可提升 cache 稳定性。 | `gw-kiro::prompt_filter` | P2 | high |
| 环境段和 billing header 噪音清洗 | `anthropic/prompt_filter.rs:95 (strip_env_noise_lines)` | 删除 `# Environment` / `# auto memory` section、`gitStatus`、`Recent commits`、`x-anthropic-billing-header` 等行。 | 对你当前的 Anthropic Messages IR 非常相关，可直接降低 system 漂移。 | `gw-kiro::prompt_filter` | P1 | high |
| 限制段大块剥离 | `anthropic/prompt_filter.rs:237 (strip_restrictions)` | 通过 regex 块删除和起止串硬剥，清掉 content_safety / git_safety / 执行约束等客户端注入段。 | 如果新网关需要“后端视角净化 system”，这是很强的参考实现，但风险较高，需谨慎开关化。 | `gw-kiro::prompt_filter` | P4 | medium |
| 用户自定义过滤规则 | `anthropic/prompt_filter.rs:262 (apply_filter_rule)` | 支持 `regex` 和 `lines-containing` 两类用户规则。 | 适合做 admin 可配过滤器，让清洗逻辑不用硬编码。 | `gw-app::admin` + `gw-kiro::prompt_filter` | P3 | high |
| bracket 风格工具调用回退解析 | `anthropic/bracket_tool_parser.rs:75 (parse_bracket_tool_calls)` | 扫描 `[Called name with args: {...}]` 文本式工具调用，解析完整 JSON 并记录 span。 | 对某些非标准模型/上游回包非常实用，能把“文本工具调用”补救成结构化 tool_use。 | `gw-kiro::parser` | P2 | high |
| bracket span 剥离 | `anthropic/bracket_tool_parser.rs:121 (strip_bracket_spans)` | 从原文本中删除已识别的 bracket tool call 文本片段。 | 非流式路径可把残留文本清掉，避免向客户端重复展示工具调用文本。 | `gw-kiro::parser` | P2 | high |
| thinking 标签真实起止识别 | `anthropic/thinking_parser.rs:46 (find_real_thinking_end_tag)`; `anthropic/thinking_parser.rs:75 (find_real_thinking_end_tag_at_buffer_end)`; `anthropic/thinking_parser.rs:99 (find_real_thinking_start_tag)` | 识别未被引号/反引号等包裹的真实 `<thinking>` 标签，避免把文档示例误判成 thinking 块。 | 如果你保留 `<thinking>` 兼容层，这组函数很值得直接借鉴。 | `gw-kiro::parser::thinking` | P1 | high |
| 完整文本中抽离 thinking | `anthropic/thinking_parser.rs:123 (extract_thinking_from_complete_text)` | 在非流式完整文本里抽出 thinking 块与剩余正文。 | 可复用到流式 buffered fallback 与非流式统一解析路径。 | `gw-kiro::parser::thinking` | P1 | high |
| thinking 解析真实接线路径 | `anthropic/handlers.rs:1578 (extract_thinking_from_complete_text 调用)`; `anthropic/stream.rs:1047 (parse_bracket_tool_calls 调用上下文)` | 非流式 handler 会从整段文本抽取 thinking；流式 parser 末尾也复用 thinking / bracket 回退逻辑。 | 说明这些模块不是死代码，而是已进请求响应主链路，参考价值高。 | `gw-kiro::stream` + `gw-kiro::parser` | P1 | high |
| prompt 预设库元数据模型 | `anthropic/prompt_presets.rs:18 (PromptPreset)`; `anthropic/prompt_presets.rs:30 (PRESETS)` | 维护一组可勾选的 system preset，按固定顺序拼接并支持 include 外部 md。 | 架构上可借鉴“preset 元数据 + 拼接顺序”，但 xkiro 内置内容偏业务/越权，具体内容不适合直接搬。 | `gw-core::prompt_preset` + `gw-app::admin` | P5 | medium |
| prompt 预设查找接口 | `anthropic/prompt_presets.rs:70 (find)`; `anthropic/prompt_presets.rs:75 (is_builtin)` | 提供 preset 查询与 builtin 判断。 | 若未来 gw-app 有 admin 配置页，这套接口可低成本复用。 | `gw-core::prompt_preset` | P5 | high |
| 前缀缓存模拟器构建 profile | `anthropic/cache_tracker.rs:70 (build_profile)` | 将 model / tool_choice / tools / system / messages 规范化为可缓存 block 序列，做 canonicalize、cache_control 提取和 prefix fingerprint 链。 | 这是 Anthropic prefix cache 模拟器的一套成熟实现，你的新项目若仍走“内部 IR=Anthropic Messages”，非常值得对照。 | `gw-kiro::cache_sim` | P1 | high |
| 账户维度缓存命中计算 | `anthropic/cache_tracker.rs:143 (compute)`; `anthropic/cache_tracker.rs:215 (update)` | 按 credential_id 存储 prefix checkpoint，支持 TTL、最近 10 个 breakpoint lookback、续期和 cache read/create 拆分。 | 可直接映射到你规划中的 `gw-kiro::cache_sim`，尤其适合多账号 Kiro 调度。 | `gw-kiro::cache_sim` | P1 | high |
| attribution header 规范化 | `anthropic/cache_tracker.rs:366 (canonicalize_system_block_for_cache)` | 将 `x-anthropic-billing-header:` 文本替换为固定占位符，避免版本漂移破坏 cache fingerprint。 | 和你当前项目的真实问题高度同构，几乎是必参考能力点。 | `gw-kiro::cache_sim` | P1 | high |
| JSON 规范化与最小可缓存 token 阈值 | `anthropic/cache_tracker.rs:490 (minimum_cacheable_tokens_for_model)`; `anthropic/cache_tracker.rs:509 (canonicalize_json)` | 以 key 排序做 canonicalize，并按 model 类型设置 1024/2048/4096 最小缓存阈值。 | 这些规则属于 cache 模拟器基础设施，能提升命中估算稳定性。 | `gw-kiro::cache_sim` | P1 | high |
| WebSearch 本地拦截判定 | `anthropic/websearch.rs:143 (has_web_search_tool)`; `anthropic/websearch.rs:220 (should_handle_websearch_request)` | 判断请求是否该走本地 web_search 处理，而非转发上游。 | 如果 claude-all-in-one 要兼容 Anthropic server_tool_use 里的 web_search，这是现成路线。 | `gw-kiro::websearch` | P3 | high |
| 混合 tools 请求剥离 web_search | `anthropic/websearch.rs:243 (strip_web_search_tools)` | 将 `web_search` 从工具列表中剔除，允许其它工具继续走上游。 | 适合实现“本地工具 + 上游工具”混合编排。 | `gw-kiro::websearch` | P3 | high |
| WebSearch 查询提取与协议桥接 | `anthropic/websearch.rs:256 (extract_search_query)`; `anthropic/websearch.rs:315 (create_mcp_request)` | 从 Anthropic Messages 提取 query，生成 MCP `tools/call` 请求。 | 对 `gw-core` IR -> Kiro MCP 的桥接很有借鉴意义。 | `gw-kiro::websearch` | P3 | high |
| WebSearch SSE 合成 | `anthropic/websearch.rs:359 (create_websearch_sse_stream)`; `anthropic/websearch.rs:384 (generate_websearch_events)` | 伪造 Anthropic 风格 `message_start` / `server_tool_use` / `web_search_tool_result` / `message_delta` SSE 流。 | 如果你要做本地 server_tool_use 仿真，这是最值钱的部分。 | `gw-kiro::stream` + `gw-kiro::websearch` | P4 | high |
| WebSearch 总处理入口 | `anthropic/websearch.rs:645 (handle_websearch_request)` | 串起 query 提取、MCP 调用、缓存 usage 结算、流式/非流式响应生成。 | 可当作“本地专用工具 handler”模板。 | `gw-kiro::websearch` | P4 | high |
| 设备指纹多字段确定性生成 | `kiro/fingerprint.rs:107 (generate_from_seed)` | 基于 seed 决定性生成 sdk/node/kiro 版本、OS、语言、屏幕、色深、并发度、时区和 machine_id。 | 作为“设备画像生成器”思路有参考性，可避免每次请求随机飘动。 | `gw-kiro::fingerprint` | P4 | medium |
| 指纹 UA 组装函数 | `kiro/fingerprint.rs:171 (user_agent)`; `kiro/fingerprint.rs:185 (x_amz_user_agent)` | 根据 `Fingerprint` 拼接 `user-agent` 与 `x-amz-user-agent`。 | 能作为 header 拼装模板，但前提是指纹对象真的进主路径。 | `gw-kiro::fingerprint` | P4 | medium |
| 指纹模块使用状态判定 | `kiro/fingerprint.rs:61 (Fingerprint)`; `kiro/endpoint/ide.rs:37 (x_amz_user_agent)`; `kiro/endpoint/ide.rs:44 (user_agent)` | `fingerprint.rs` 只定义了结构与生成函数；实际请求路径里的 IDE endpoint 直接用 `ctx.config.kiro_version`、`ctx.config.system_version`、`ctx.config.node_version` 和 `ctx.machine_id` 手拼 UA，没有调用 `Fingerprint::generate_from_seed` / `user_agent` / `x_amz_user_agent`。全仓 `rg` 也只找到 `fingerprint.rs` 自身定义与测试。 | 结论：这是半成品/未接线模块，当前不在真实请求主链路。若 claude-all-in-one 要做设备画像，应按真实 header 装配路径重新设计接线，而不是直接照搬。 | `gw-kiro::fingerprint` + `gw-kiro::machine_id` + `gw-kiro::egress` | P4 | high |
| 独立会话亲和表 | `kiro/affinity.rs:33 (new)`; `kiro/affinity.rs:41 (get)`; `kiro/affinity.rs:53 (set)`; `kiro/affinity.rs:68 (touch)` | 独立维护 `session_id -> credential_id` 绑定，带 TTL、LRU 驱逐、按 credential 清除等能力。 | 这是你新架构里 `scheduler` 非常值得单拆的能力点，模块边界清晰。 | `gw-kiro::scheduler::affinity` | P1 | high |
| 亲和模块真实接线路径 | `kiro/token_manager.rs:668 (session_affinity 字段)`; `kiro/token_manager.rs:880 (SessionAffinity::default)` | `SessionAffinity` 已被 `MultiTokenManager` 持有并初始化，说明它不是死代码，属于 token/scheduler 主链的一部分。 | 证明“独立亲和模块”方案在 xkiro 已落地，适合你在新项目里保留这种解耦设计。 | `gw-kiro::scheduler` | P1 | high |
| 后台刷新器配置校验与生命周期 | `kiro/background_refresh.rs:48 (validate)`; `kiro/background_refresh.rs:84 (new)`; `kiro/background_refresh.rs:106 (start)`; `kiro/background_refresh.rs:209 (stop)` | 把 token refresh 做成独立后台任务，支持配置校验、重复启动保护、批处理、并发限制和优雅停止。 | 很适合你规划中的 `gw-app(worker)` 进程使用，避免请求路径同步刷新。 | `gw-kiro::token::background_refresh` | P2 | high |
| 刷新结果抽象 | `kiro/background_refresh.rs:235 (RefreshResult)` | 将刷新结果显式区分成功/失败/fallback，并带新过期时间。 | 有助于 worker/admin/metrics 统一消费刷新状态。 | `gw-core::token_contract` 或 `gw-kiro::token` | P3 | high |
| 后台刷新真实接线路径 | `kiro/token_manager.rs:3438 (start_background_refresh)`; `kiro/token_manager.rs:3479 (refresh_token_for_credential)` | `MultiTokenManager` 会停止旧任务后启动 `BackgroundRefresher`，并把具体 refresh 逻辑注入进去，失败时支持 fallback 继续用未过期 token。 | 说明后台刷新不是孤立工具，而是已融入 token manager 主路径；非常适合迁移为 `gw-app worker + gw-store` 协作。 | `gw-app::worker` + `gw-kiro::token` + `gw-store` | P2 | high |
| Kiro Web Portal CBOR RPC 客户端 | `kiro/web_portal.rs:141 (build_headers)`; `kiro/web_portal.rs:174 (request_cbor)` | 对 `app.kiro.dev/service/KiroWebPortalService/operation/*` 发送 `rpc-v2-cbor` 请求，带 Authorization + Cookie + smithy headers，并解析 CBOR 错误体。 | 这是“服务端查额度/订阅”最核心的参考实现，协议细节完整。 | `gw-kiro::web_portal` | P1 | high |
| 获取用户信息 / 用量接口 | `kiro/web_portal.rs:223 (get_user_info)`; `kiro/web_portal.rs:240 (get_user_usage_and_limits)` | 分别查询用户基本信息和额度/订阅/用量。 | 可直接用于 admin 面板或调度器余额感知。 | `gw-kiro::web_portal` | P1 | high |
| 账户额度聚合与订阅归一化 | `kiro/web_portal.rs:399 (aggregate_account_info)`; `kiro/web_portal.rs:344 (norm_subscription_type)`; `kiro/web_portal.rs:371 (free_trial_is_effective)`; `kiro/web_portal.rs:382 (bonus_is_effective)` | 将 raw usage 响应聚合成 credits 总览、resource 明细、bonus/free-trial 过滤结果和 subscription 摘要。 | 这非常适合落到 `gw-app::admin`，省去前端重复聚合逻辑，也利于调度策略消费。 | `gw-kiro::web_portal` + `gw-app::admin` | P2 | high |
| 通用 HTTP client 构建器 | `http_client.rs:46 (build_client)` | 统一构建 reqwest client，支持 timeout、代理认证和 TLS backend 选择。 | 这是新项目所有外呼都该有的公共基建。 | `gw-core::http` 或 `gw-kiro::http` | P1 | high |
| 代理配置值对象 | `http_client.rs:12 (ProxyConfig)`; `http_client.rs:22 (new)`; `http_client.rs:31 (with_auth)` | 将代理 URL 与认证封装为独立配置对象。 | 适合 `gw-core` 契约化，供 web portal / token refresh / egress 共用。 | `gw-core::config` | P1 | high |
| HTTP client 真实复用范围 | `http_client.rs:46 (build_client)`; `kiro/provider.rs:98 (build_client 调用)`; `kiro/web_portal.rs:189 (build_client 调用)`; `token.rs:172 (build_client 调用)`; `kiro/token_manager.rs:167 (build_client 调用)` | `build_client` 已被 provider、web_portal、token 统计与 token_manager 多处复用，说明它是成熟公共基础设施。 | 证明把 HTTP 构建器下沉为共享模块是对的，建议在 claude-all-in-one 开局就抽到公共层。 | `gw-core::http` | P1 | high |
| tool 压缩真实接线路径 | `anthropic/converter.rs:668 (compress_tools_if_needed 调用)` | 在 placeholder tool 补齐之后、构建 `UserInputMessageContext` 之前，对最终 tools 列表执行压缩。 | 说明工具压缩已真正进入请求主链，可直接作为你新项目 converter 阶段的参考时序。 | `gw-kiro::converter` | P1 | high |
| prompt 过滤真实接线路径 | `anthropic/converter.rs:1439 (base_system 构建)` | `apply_prompt_filters` 在 system 拼装初期逐 block 执行，然后再叠加 chunked policy、thinking prefix、tool docs 与 truncation notice。 | 说明 prompt 清洗应放在 system 合成早期，而不是最后才做。 | `gw-kiro::converter` | P1 | high |
| truncation 检测真实接线路径 | `anthropic/handlers.rs:1425 (detect_truncation 调用)`; `anthropic/handlers.rs:1452 (required_fields_for 调用)` | 解析工具输入 JSON 失败时先做截断检测；即便 JSON 解析成功，也会对关键工具再做必填字段缺失检查。 | 说明“截断检测”不只是工具库，而是已嵌入响应解析主路径，值得按这个顺序移植。 | `gw-kiro::parser` | P1 | high |
| bracket tool parser 真实接线路径 | `anthropic/handlers.rs:1545 (parse_bracket_tool_calls 调用)`; `anthropic/stream.rs:1047 (parse_bracket_tool_calls 调用)` | 非流式路径会抽取并剥离 bracket 文本；流式路径会在末尾补发结构化 tool_use blocks。 | 说明这是完善的双路径能力，不是实验代码。 | `gw-kiro::parser` + `gw-kiro::stream` | P2 | high |
| WebSearch 接入主路由 | `anthropic/handlers.rs:922 (should_handle_websearch_request 调用)`; `anthropic/handlers.rs:933 (handle_websearch_request 调用)` | Anthropic handlers 在常规上游转发前就短路到本地 web_search 处理。 | 这可作为你新项目“本地工具优先级”的接入点模板。 | `gw-app::router` + `gw-kiro::websearch` | P3 | high |
