# static_flow stream + parser 簇 — 实现参考

| 能力点 | 参考来源(文件:行号 函数名) | 做什么 | 对claude-all-in-one价值 | 落到claude-all-in-one模块 | 建议Phase | confidence |
|---|---|---|---|---|---|---|
| 流适配总导出面 | `anthropic/stream/mod.rs:23-31 (pub use ...)` | 对外统一暴露 `StreamContext`、`BufferedStreamContext`、thinking 签名、inline thinking、usage 解析与 `SseEvent` | 适合作为 claude-all-in-one 的 stream façade，避免 app 层直接依赖子模块细节 | gw-kiro/stream | P1 | high |
| 标准 SSE 事件值对象 | `anthropic/stream/sse_event.rs:11 (SseEvent::new)`, `anthropic/stream/sse_event.rs:20 (SseEvent::to_sse_string)` | 用 `{event,data}` 抽象 SSE，并负责格式化为 `event: ...\ndata: ...\n\n` | claude-all-in-one 可直接复用同样的 wire value，简化 router/egress 输出 | gw-kiro/stream | P1 | high |
| SSE 协议状态机骨架 | `anthropic/stream/state.rs:50 (SseStateManager::new)`, `anthropic/stream/state.rs:100 (handle_message_start)`, `anthropic/stream/state.rs:108 (handle_content_block_start)`, `anthropic/stream/state.rs:141 (handle_content_block_delta)`, `anthropic/stream/state.rs:153 (handle_content_block_stop)`, `anthropic/stream/state.rs:166 (generate_final_events)` | 维护 message/content_block 的 start/delta/stop 生命周期，并统一生成 `message_delta`/`message_stop` | 是 Anthropic Messages IR 输出层的核心，可避免事件时序错乱 | gw-kiro/stream | P1 | high |
| block reopen/类型校验 | `anthropic/stream/state.rs:63 (is_block_open_of_type)` | 校验某 index 当前是否仍是指定 block type 且未关闭 | 解决 text/tool_use/thinking 交错后写到错误 block 的问题 | gw-kiro/stream | P1 | high |
| block index 分配器 | `anthropic/stream/state.rs:69 (next_block_index)` | 顺序分配 content block index | Anthropic SSE 必需；建议在 claude-all-in-one 中保持单调 index 分配 | gw-kiro/stream | P1 | high |
| tool_use 出现时提前关闭 text block | `anthropic/stream/state.rs:108 (handle_content_block_start)` | 当新 block 是 `tool_use` 时，先停止所有仍打开的 text block | 避免 text 与 tool_use 并发打开，和 Claude 客户端预期一致 | gw-kiro/stream | P1 | high |
| stop_reason 归一规则 | `anthropic/stream/state.rs:79 (set_stop_reason)`, `anthropic/stream/state.rs:90 (get_stop_reason)` | stop_reason 优先级：显式原因 > `tool_use` > `end_turn` | 给 claude-all-in-one 一套稳定的 stop reason 生成规则 | gw-kiro/stream | P1 | high |
| 检测“只有 thinking 没有普通内容” | `anthropic/stream/state.rs:84 (has_non_thinking_blocks)` | 判断当前是否存在非 thinking block | 用于空响应兜底和 `max_tokens` stop_reason 补偿 | gw-kiro/stream | P2 | high |
| input token 用量 JSON 形状 | `anthropic/stream/usage.rs:22 (anthropic_usage_json)` | 产出 Anthropic 兼容的 `usage`，含 `input_tokens/output_tokens/cache_creation_input_tokens/cache_read_input_tokens` | claude-all-in-one 的 Messages IR 既然走 Anthropic，usage 输出格式必须对齐 | gw-kiro/stream | P1 | high |
| request token 与 contextUsage 的判定阈值 | `anthropic/stream/usage.rs:18 (KIRO_CONTEXT_USAGE_MIN_REQUEST_TOKENS)`, `anthropic/stream/usage.rs:41 (resolve_input_tokens)`, `anthropic/stream/usage.rs:52 (resolve_input_tokens_with_threshold)` | 小请求优先信本地 request estimate，大请求再信 upstream `contextUsage` | 能显著降低 Kiro bridge/system prompt 膨胀导致的 input token 虚高 | gw-kiro/stream | P1 | high |
| protected thinking 签名上下文 | `anthropic/stream/signature.rs:63 (ThinkingSignatureContext::new)`, `anthropic/stream/signature.rs:70 (ThinkingSignatureContext::signature)` | 将 `key_id + secret` 绑定到 thinking 签名生成 | claude-all-in-one 若要做“服务端可验证签名”或回放校验，这是非常好的接口形状 | gw-kiro/stream | P2 | high |
| synthetic/protected thinking signature 生成 | `anthropic/stream/signature.rs:284 (synthetic_thinking_signature)`, `anthropic/stream/signature.rs:293 (protected_thinking_signature)` | 生成 Anthropic 形状的 base64 protobuf 签名，支持无 secret 合成或带 secret 保护 | 若上游不给真签名，claude-all-in-one 仍可稳定输出 Claude 兼容 thinking block | gw-kiro/stream | P1 | high |
| thinking 签名校验与 legacy 兼容 | `anthropic/stream/signature.rs:320 (verify_protected_thinking_signature)`, `anthropic/stream/signature.rs:304 (legacy_protected_thinking_signature)` | 验证当前/旧版 envelope 形状的 protected 签名 | 对历史会话回放、持久化恢复、admin 诊断都很有价值 | gw-kiro/stream | P3 | medium |
| protobuf envelope 构造细节可参考 | `anthropic/stream/signature.rs:217 (build_signature_envelope)`, `anthropic/stream/signature.rs:229 (build_signature_envelope_with_shape)` | 把 header/body/nonce/proof 组装成和 Claude Code 观测一致的 envelope | claude-all-in-one 若要 1:1 模拟 Anthropic thinking 签名格式，可直接借鉴字段布局 | gw-kiro/stream | P2 | high |
| HMAC 保护签名字节派生 | `anthropic/stream/signature.rs:143 (hmac_sha512)`, `anthropic/stream/signature.rs:173 (derive_protected_signature_bytes)` | 用服务端 secret 派生稳定签名字节 | 可用于“同 thinking、同 key、同 model 得到稳定签名”的可验证方案 | gw-kiro/stream | P3 | high |
| deterministic synthetic 签名字节派生 | `anthropic/stream/signature.rs:119 (derive_deterministic_signature_bytes)` | 不依赖 secret，只依据 model/thinking/label 生成稳定字节 | 适合本地开发/无 secret 部署环境 | gw-kiro/stream | P1 | high |
| inline `<thinking>` 内容块生成 | `anthropic/stream/inline_thinking.rs:17 (build_inline_thinking_content_blocks)`, `anthropic/stream/inline_thinking.rs:30 (build_inline_thinking_content_blocks_with_signature_context)` | 把混在 assistant text 里的 `<thinking>...</thinking>` 切分为 thinking/text blocks，并自动附签名 | 当 Kiro 只把 thinking 内联进文本时，claude-all-in-one 仍能还原出 Anthropic block IR | gw-kiro/stream | P1 | high |
| quoted/escaped thinking tag 过滤 | `anthropic/stream/inline_thinking.rs:64 (is_quote_char)`, `anthropic/stream/inline_thinking.rs:128 (find_real_tag)` | 过滤被引号/转义包裹的伪 tag，避免误判 thinking 边界 | 减少普通代码/文本里出现 `<thinking>` 时被错误切块 | gw-kiro/stream | P2 | high |
| thinking 起始边界识别 | `anthropic/stream/inline_thinking.rs:74 (find_real_thinking_start_tag)` | 找到真正的 `<thinking>` 起点 | stream chunk 跨界时的核心判定器 | gw-kiro/stream | P1 | high |
| thinking 结束边界识别（流式） | `anthropic/stream/inline_thinking.rs:80 (find_real_thinking_end_tag)` | 只有遇到 `</thinking>\n\n` 才认为 thinking mid-stream 完整结束 | 避免半截结束标签导致过早 closing thinking block | gw-kiro/stream | P1 | high |
| thinking 结束边界识别（buffer 尾部/flush） | `anthropic/stream/inline_thinking.rs:106 (find_real_thinking_end_tag_at_buffer_end)` | 允许以 trailing whitespace 结束，用于 tool_use 前/最终 flush 时补关 thinking block | 是解决“chunk 末尾刚好停在 `</thinking>`”这类边界问题的关键 | gw-kiro/stream | P1 | high |
| 移除 inline thinking，仅保留对用户可见文本 | `anthropic/stream/inline_thinking.rs:201 (strip_inline_thinking_content)` | 去掉 `<thinking>` 段，只保留外层文本 | hidden thinking 模式下必须要有的清洗器 | gw-kiro/stream | P2 | high |
| stream 核心上下文对象 | `anthropic/stream/context.rs:96 (StreamContext::new_with_thinking)`, `anthropic/stream/context.rs:142 (new_with_thinking_visibility)`, `anthropic/stream/context.rs:161 (new_with_identity)` | 聚合 SSE 状态、thinking 解析、tool_use 聚合、usage/metering、structured output、identity rewrite | claude-all-in-one 最值得整体借鉴的“单请求流处理器”骨架 | gw-kiro/stream | P1 | high |
| 可配置 contextUsage 阈值 | `anthropic/stream/context.rs:187 (with_context_usage_min_request_tokens)` | 将 token 判定阈值下沉为 per-context 配置 | 为 admin 热调或不同模型策略留下插口 | gw-kiro/stream | P2 | high |
| thinking 签名上下文注入点 | `anthropic/stream/context.rs:192 (with_thinking_signature_context)` | 给 `StreamContext` 注入 protected/synthetic 签名策略 | claude-all-in-one 可以把签名策略解耦成依赖注入，而非硬编码 | gw-kiro/stream | P2 | high |
| structured output 模式分支 | `anthropic/stream/context.rs:200 (structured_output_mode)`, `anthropic/stream/context.rs:729 (process_tool_use)` | 若命中特定 structured-output tool，则不发 tool_use block，而是聚合 JSON 并转成 text | 对兼容 Claude Code/JSON-only 客户端很有参考价值 | gw-kiro/stream | P2 | high |
| hidden thinking 开关 | `anthropic/stream/context.rs:204 (thinking_parser_enabled)`, `anthropic/stream/context.rs:208 (final_assistant_text)` | 支持解析 thinking 但不向下游暴露，只在最终文本中剥离 | 适合实现“上游有 reasoning，但下游用户不可见”的策略 | gw-kiro/stream | P2 | high |
| 最终 usage 口径统一 | `anthropic/stream/context.rs:216 (final_usage)` | 最终 `input_tokens/output_tokens` 统一从 request/contextUsage 阈值逻辑与最小 output=1 得出 | 避免 stream/non-stream 两套 usage 逻辑漂移 | gw-kiro/stream | P1 | high |
| credit metering 汇总 | `anthropic/stream/context.rs:233 (final_credit_usage)`, `anthropic/stream/context.rs:390 (process_kiro_event)` | 汇总 `meteringEvent` 中 `credit` 单位 usage，并记录是否观测到 | 适合 claude-all-in-one 后续对账/计费统计/usage 存储 | gw-kiro/stream | P2 | high |
| identity response rewrite 挂点 | `anthropic/stream/context.rs:179 (with_response_identity)`, `anthropic/stream/context.rs:245 (apply_identity_response)`, `anthropic/stream/context.rs:257 (canonical_identity_thinking)` | 对特定身份探针响应做 canonical rewrite，连 thinking 也能一起改写 | 虽然是更高层需求，但 stream 层预留这种注入点很有价值 | gw-kiro/stream | later | medium |
| thinking 签名生成统一入口 | `anthropic/stream/context.rs:263 (thinking_signature)` | 根据是否存在 `ThinkingSignatureContext` 自动走 protected 或 synthetic 签名 | claude-all-in-one 不必在上层判断签名模式，接口简洁 | gw-kiro/stream | P1 | high |
| 无原始 thinking 时主动合成 thinking block | `anthropic/stream/context.rs:270 (should_synthesize_thinking_block)`, `anthropic/stream/context.rs:278 (synthesize_thinking_block)` | 在开启 thinking 但上游未给可见 thinking 的情况下，仍补一个空/占位 thinking block 与签名 | 对抗“空响应但客户端期待 thinking block”很关键 | gw-kiro/stream | P2 | high |
| final assistant message 聚合 | `anthropic/stream/context.rs:305 (final_assistant_message)` | 将累积文本和已完成 tool_uses 还原成最终 `AssistantMessage` | 这正好契合你的“内部 IR 走 Anthropic Messages”架构 | gw-core + gw-kiro/stream | P1 | high |
| final content blocks 聚合 | `anthropic/stream/context.rs:319 (final_content_blocks)` | 依据 completed thinking / inline thinking / visible text 生成最终 content blocks | 非流式接口和持久化 replay 都能直接消费 | gw-kiro/stream | P1 | high |
| `message_start` 事件生成 | `anthropic/stream/context.rs:347 (create_message_start_event)` | 生成 Anthropic `message_start`，并预填 usage | claude-all-in-one 需要在真正接收 chunk 前先发起标准 Anthropic 流头 | gw-kiro/stream | P1 | high |
| 初始事件生成策略 | `anthropic/stream/context.rs:365 (generate_initial_events)` | 先发 `message_start`；在非 thinking/非 structured-output/非 identity 模式下，预开 text block | 下游体验更稳定，且对 Anthropic SSE 时序更友好 | gw-kiro/stream | P1 | high |
| Kiro event 分发入口 | `anthropic/stream/context.rs:390 (process_kiro_event)` | 把 `AssistantResponse/ReasoningContent/ToolUse/ContextUsage/Metering/Exception` 分发到对应处理器 | 这是 claude-all-in-one stream adaptor 的主 dispatch 点 | gw-kiro/stream | P1 | high |
| assistantResponse 内容处理 | `anthropic/stream/context.rs:437 (process_assistant_response)` | 处理普通文本 chunk，支持 structured output、identity rewrite、thinking parser、纯文本模式 | Kiro 上游最常见事件路径，必须落稳 | gw-kiro/stream | P1 | high |
| reasoningContentEvent 直通 thinking 文本 | `anthropic/stream/context.rs:467 (process_reasoning_content)` | 当上游有独立 reasoningContentEvent 时，开 thinking block，增量发 `thinking_delta`，收到 signature 时收尾 | 这是“thinking 签名透传/归一化”的最核心参考实现 | gw-kiro/stream | P1 | high |
| identity 模式下 reasoning 内容改写 | `anthropic/stream/context.rs:516 (process_identity_reasoning_content)` | reasoningContentEvent 也可被 canonical identity thinking 覆盖 | 说明 thinking 与最终文本都可在 stream 层统一改写 | gw-kiro/stream | later | medium |
| 内联 thinking 流式解析器 | `anthropic/stream/context.rs:558 (process_content_with_thinking)` | 面向 chunk 流的 `<thinking>` 状态机：识别开始/结束、缓存半截 tag、区分 thinking/text 输出 | 这是你要做的 inline thinking parser 的最关键参考 | gw-kiro/stream | P1 | high |
| 文本 delta 事件生成与 block 重开 | `anthropic/stream/context.rs:655 (create_text_delta_events)` | 在 text block 已关闭或不存在时自动重开，再发送 `text_delta` | 可稳住 tool_use 后重新继续文本输出的场景 | gw-kiro/stream | P1 | high |
| thinking delta 事件生成与内容累积 | `anthropic/stream/context.rs:683 (create_thinking_delta_event)` | 累积开放中的 thinking 文本，并发 `thinking_delta` | 构建最终 thinking block 和签名前的关键缓存点 | gw-kiro/stream | P1 | high |
| thinking block 收尾并发 signature_delta | `anthropic/stream/context.rs:693 (finalize_open_thinking_block)`, `anthropic/stream/context.rs:697 (finalize_open_thinking_block_with_signature)` | 把开放中的 thinking 文本固化为 completed_thinking，发 `signature_delta` 后再 stop block | Anthropic thinking 流的关键时序就是“thinking_delta -> signature_delta -> content_block_stop” | gw-kiro/stream | P1 | high |
| tool_use 增量 JSON 聚合 | `anthropic/stream/context.rs:729 (process_tool_use)` | 聚合同一 `tool_use_id` 的 partial JSON，发 `input_json_delta`，stop 时再反序列化为最终 `ToolUseEntry` | 对 Claude Code 工具调用兼容极其重要 | gw-kiro/stream | P1 | high |
| tool_name 恢复映射 | `anthropic/stream/context.rs:805-828 (process_tool_use)` | 将 Kiro 侧缩短/改写过的 tool 名恢复成原始名字 | 解决 Kiro 上游 tool 名限制与 Anthropic tool schema 名称不一致的问题 | gw-kiro/stream | P2 | high |
| structured output tool 转文本 | `anthropic/stream/context.rs:730-749 (process_tool_use)`, `anthropic/stream/context.rs:44 (canonicalize_structured_output_json)` | 某个特殊 tool 的 JSON 输出不当作 tool_use 暴露，而 canonicalize 后转成 text | 对想把“结构化输出”伪装成普通 assistant text 的兼容层很有帮助 | gw-kiro/stream | P2 | high |
| tool_use 前强制 flush thinking/text buffer | `anthropic/stream/context.rs:752-795 (process_tool_use)` | tool_use 到来前，先补关 thinking block，或把残留文本刷出 | 这是处理“thinking/text 尚未完整收尾就来 tool_use”的关键兜底 | gw-kiro/stream | P1 | high |
| 最终 flush 与空响应兜底 | `anthropic/stream/context.rs:870 (generate_final_events)` | 在结束时 flush structured output、flush 半截 thinking/text、必要时 synthesize thinking；若只有 thinking 则补单空格 text，并设 `max_tokens` | 这是你特别关注的“空响应兜底”最有价值的参考逻辑 | gw-kiro/stream | P1 | high |
| thinking-only 响应补空格文本 | `anthropic/stream/context.rs:941-947 (generate_final_events)` | 若只有 thinking 没有非 thinking block，则补一个 `