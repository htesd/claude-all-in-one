# caio 交接 — 2026-07-28

## 1. 生产现状(已上线)

| 容器 | 镜像 | 上线时间 |
|---|---|---|
| caio-worker0 | `claude-all-in-one:think-floor` | 14:21 |
| caio-router | `claude-all-in-one:groupv2` | 13:39 |
| caio-*-dario | `c25254c7c5ce`(旧,未动) | — |

已上线内容:①账号-分组多对多重构 ②对外错误脱敏 + 公网 `/health` 收窄 ③思考预算下限(8192)。
成功率 99.7%,零 503。

**回滚**:
```bash
cd /root/claude-all-in-one && sed -i 's|claude-all-in-one:think-floor|claude-all-in-one:rollback-groupv2-worker-20260728-1310|' docker-compose.yml && docker compose up -d
```
备份在 `/root/caio-backup/`:整库 `control-20260728-1310.db`、四张表 SQL dump、`docker-compose-*.yml`、
`credit-baseline-before-1.0.212.txt`、`kiro-models-1.0.212.json`。

## 2. 未提交改动(分支 refactor/account-group-membership,830 测试绿)

`crates/gw-kiro/` 6 个文件,全部**前缀安全**(不动缓存前缀):
- `kiro_types/request.rs`:`KiroRequest` 加 `additional_model_request_fields` + `agent_mode`(必发,"vibe")
- `kiro_types/conversation.rs`:当前消息的 `userInputMessageContext` 加 `skip_serializing_if`
  (此前每个无工具请求都多发 `{}`,真客户端从不发 —— GPT 找到的高危)
- `thinking_policy.rs`:`additional_model_request_fields()` / `effective_effort()` / `budget_to_effort()`
- `converter/history.rs`:`generate_thinking_prefix` 默认返回 None,`KIRO_LEGACY_THINKING_TAGS=1` 回退
- `headers.rs`:`DEFAULT_KIRO_VERSION` 0.12.155 → **1.0.212**
- `chat.rs`:组装新字段

**未部署**。

## 3. ⚠️ 我今天引入的 bug,必须先修

`anthropic_types.rs` 的 `EFFORT_ALIASES` 把 `max` → `xhigh`。**错的**:
真实 enum 是 `["low","medium","high","xhigh","max"]`,`max` 是**最高档**,我把它降了一级。
生产每 300 条请求里有 33 条发 `max`。

而且档位**每模型不同**:`opus-4.6`/`sonnet-4.6` 是 `low,medium,high,max`(**无 xhigh**),
caio 对所有 Opus 硬编码默认 `xhigh` → 对 4.6 系发了它不认的档。

## 4. Kiro 1.0.212 拆包结论

客户端 deb `1.0.212-1784842874`,commit `8848ae36`,build 2026-07-23。
bundle:`/usr/share/kiro/resources/app/extensions/kiro.kiro-agent/dist/extension.js`
(22 MB / 536,408 行,最长行 274,114 字符)。

### 端点表

| 操作 | caio 现在 | 1.0.212 真实 | 判定 |
|---|---|---|---|
| `generateAssistantResponse` | `runtime.{region}.kiro.dev`(默认) | 同 | ✅ 已对齐 |
| `getUsageLimits` | `q.{region}.amazonaws.com`(写死) | **`management.{region}.kiro.dev`** | ❌ 要改 |
| `ListAvailableModels` | 未实现 | `management.{region}.kiro.dev` | 新增 |
| `GetProfile` | 未用 | `management.{region}.kiro.dev` | 待评估 |
| `ListAvailableProfiles` | `q.{region}.amazonaws.com` | 走 VS Code 命令转发,未定位 | ⚠️ 待查 |

解析器:`extension.js:217247` `bi2()` → `https://management.{region}.kiro.dev`,
调用点 223706(Models)/314012+314158(GetProfile)/337354(UsageLimits)。
运行时域:`:227130` `https://runtime.${region}.kiro.dev`。

**切换风险已验**:caio 解析的 6 个字段(`subscriptionTitle`/`currentUsageWithPrecision`/
`usageLimitWithPrecision`/`bonuses`/`freeTrialInfo`/breakdown 条数)在新老端点**逐字相同**;
新端点大小写路径都接受。差异字段(`daysUntilReset`/`limits`/`totalUsage`/`upgradeCapability`)caio 不读。

### thinking 机制换了

`<thinking_mode>/<thinking_effort>/<max_thinking_length>` 在 1.0.212 **全树零命中**。新做法
(`extension.js:222579` 的 `qe8`):顶层 `additionalModelRequestFields`,形态由每模型
`schemaPath` 决定(`output_config` 或 `reasoning`)。

opus-5 的真实 schema(`additionalProperties: false` **严格**):
```json
{"thinking":{"type":["adaptive","disabled"],"display":["summarized","omitted"],"required":["type"]},
 "output_config":{"effort":{"enum":["low","medium","high","xhigh","max"],"default":"high"}},
 "max_tokens":{"minimum":1024,"maximum":128000}}
```
`thinking.type` 无 `enabled` → 上游确无 budget 概念,budget→档位翻译方向正确。
**caio 目前只发 `output_config`,缺 `thinking` 与 `max_tokens`。**

真机 A/B(同一道证明题,claude-opus-5,数 `reasoningContentEvent` 帧):

| 方式 | 耗时 | reasoning 帧 |
|---|---|---|
| 新字段 low | 33 s | 137 |
| 基准(不加控制) | 75 s | 410 |
| 新字段 xhigh | 169 s | 1406 |
| 旧标签+新字段 | 235 s | 2052 |
| **旧标签 xhigh** | **280 s** | **2651** |

→ 旧标签**更深**,但用户已决定「同步优先于深度」。

### 请求体形态

真实:`{conversationState, profileArn, additionalModelRequestFields, agentMode, systemPrompt}`
(`extension.js:228361`/`:228453`,流式与非流式都无条件带 `agentMode`)。

**`systemPrompt` 不用做** —— `extractSystemPrompt` 受 A/B 开关 `AB_SYSTEM_FIELD_INJECTION` 控制,
`:227083` 默认 **false**。关着时 system 留在 history[0] = **caio 现有做法**,对齐的是对照组。
(用户当初这么做是为了省积分/保缓存,是对的。)

### 计费倍率表(`/root/caio-backup/kiro-models-1.0.212.json`,19 个模型)

opus-5/4.8/4.7/4.6 = **2.2** | sonnet 系 = 1.3 | haiku-4.5 = 0.4 | auto = 1.0
gpt-5.6-sol/terra/luna = 2.4/1.2/0.6 | glm-5 = 0.5 | deepseek-3.2 & minimax-m2.5 = 0.25
minimax-m2.1 = 0.15 | **qwen3-coder-next = 0.05**
opus-5 `promptCaching`: 最多 4 个 checkpoint,每个最少 1024 token。

### 其它已确认
- 5 个 header middleware caio 全有,**无新增头**;`agentMode` 默认仍 `vibe`
- caio 未解析的响应事件:`toolResultEvent` `metadataEvent`(带 stopReason)`codeReferenceEvent`
  `documentCitationEvent` `supplementaryWebLinksEvent`
- `real_cache_read_tokens` 历史上从未非零(上游不发 `tokenUsageEvent`),既存盲点,非回归
- **不要补** `cursorState`/`editorState`/`workspaceFolders`/`currentWorkingDirectory` ——
  这条 converse 路径本就不发,补了反而造出新异常形态

## 5. 💰 经济约束(决定改动排期)

用户原话:500k 上下文**无缓存命中 5-6 积分,命中 2-3 积分**,直接决定赚钱还是亏。

**积分基准(改动前,近 2 小时)** — `/root/caio-backup/credit-baseline-before-1.0.212.txt`:

| 模型 | 请求数 | 每请求积分 | 模拟命中率 |
|---|---|---|---|
| claude-opus-5 | 5527 | **0.9584** | **83.4%** |
| claude-opus-4-8 | 1543 | 1.1316 | 83.9% |
| claude-sonnet-5 | 599 | 0.3881 | 70.2% |

**改动按是否动前缀分两批**:
- **前缀安全**(可随时上):`agentMode`、`additionalModelRequestFields`、UA 版本、
  去空 `userInputMessageContext`(只动当前消息=前缀尾巴之后)、`getUsageLimits` 换域
- **前缀破坏**(必须低峰单独切):**移除 history[0] 里的 thinking 标签** —— history[0] 是前缀
  第一块,改它 = 所有在途会话下一轮全 miss。一次性代价;长期收益是 history[0] 不再随 effort 抖动。

## 6. 待办

1. 修 `max` 降级 bug(见 §3)
2. `additionalModelRequestFields` 补 `thinking:{type,display}` + `max_tokens`
3. effort 档位改按模型取(接 `ListAvailableModels`),别硬编码
4. 接 `ListAvailableModels`,`rateMultiplier` 落库供定价
5. `getUsageLimits` 换 `management` 域
6. 部署上述(前缀安全批)
7. 低峰单独切 history[0] 标签移除,盯积分对比基准
8. GPT 端点报告未完成(task `bzq69rd6p`),要覆盖 `ListAvailableProfiles` 与两条 token 刷新归属

## 7. ⚠️ 操作纪律

**本机编译必须限内存** —— 今天 `cargo test --workspace`(14 核默认 `-j14`)把 30G 吃穿,
swap livelock 硬死机重启(内核无 OOM-kill 记录,全是 `swap_reclaim_work hogged CPU` 到 259 次)。
此后一律:
```bash
systemd-run --user --scope -p MemoryMax=8G -p MemorySwapMax=0 -- nice -n 15 cargo test -j 4 --workspace
```
拆 bundle 只用 `awk`(流式,内存恒定);**禁** `grep -r`/`ugrep -r` 递归、禁 js-beautify、
禁把整文件读进字符串。另:`pkill -f <模式>` 会匹配到自己的 shell,已踩两次。

**凭据**:服务器口令仅本地用,绝不发给 codex/第三方;发 diff 给 codex 前必扫。
**Kiro 账号**:只用只读控制面调用验证,别反复打 chat(会触发 TEMPORARILY_SUSPENDED)。
